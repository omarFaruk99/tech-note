# 🐳 Docker Mastery Guide for React Developers (Bangla)

এই গাইডটি বিশেষভাবে তাদের জন্য তৈরি করা হয়েছে যারা Windows-এ React (Vite) ব্যবহার করেন এবং Docker সম্পর্কে একদম জিরো থেকে নলেজ নিতে চান। এই নোটটি পড়ার পর আপনি আপনার প্রজেক্টকে Dockerize করতে এবং প্রফেশনাল লেভেলে ব্যবহার করতে পারবেন।

---

## 🟢 Phase 1: Docker এর অ আ ক খ (The Basics)

### ১. Docker কি? (What is Docker?)

সহজ কথায়, Docker হলো এমন একটি টেকনোলজি যা আপনার কোড এবং তার চলার জন্য যা যা প্রয়োজন (Node, Modules, OS Settings) - সবকিছুকে একটি "বক্স" বা **Container** এর মধ্যে বন্দি করে দেয়।

**কেন ব্যবহার করবেন?**

- **"My computer-এ চলে কিন্তু server-এ চলে না"** - এই সমস্যা চিরতরে দূর হয়।
- প্রজেক্ট সেটআপ করতে ঘণ্টার পর ঘণ্টা সময় বাঁচে।
- পিসিতে Node.js এর মাল্টিপল ভার্সন নিয়ে ঝামেলা পোহাতে হয় না।

### ২. গুরুত্বপূর্ণ কিছু কি-ওয়ার্ড (Key Concepts)

- **Image:** এটি হলো আপনার প্রজেক্টের একটি "ব্লু-প্রিন্ট" বা রেসিপি। এটি নিজে চলে না, কিন্তু এটি থেকে কন্টেইনার তৈরি হয়।
- **Container:** ইমেজ থেকে যখন প্রজেক্টটি রান করা হয়, তখন সেই রান হওয়া অবস্থাকে কন্টেইনার বলে। এটি একটি ভার্চুয়াল কম্পিউটারের মতো।
- **Dockerfile:** এটি একটি টেক্সট ফাইল যেখানে ইমেজ তৈরি করার ইনস্ট্রাকশন লেখা থাকে।

---

## 🔵 Phase 2: Windows-এ Docker সেটআপ (Installation)

Windows-এ Docker চালানোর সবচেয়ে ভালো উপায় হলো **WSL2 (Windows Subsystem for Linux)** ব্যবহার করা।

### সেটআপ স্টেপস:

1. **WSL2 Enable করা:** PowerShell অ্যাডমিন হিসেবে রান করে এই কমান্ডটি দিন:
   ```bash
   wsl --install
   ```
   এরপর উইন্ডোজ রিস্টার্ট দিন।
2. **Docker Desktop ডাউনলোড:** [Docker Personal](https://www.docker.com/products/docker-desktop/) থেকে ডাউনলোড করে ইনস্টল করুন।
3. **Settings কনফিগারেশন:**
   - Docker Desktop ওপেন করে Settings-এ যান।
   - **Resources > WSL Integration**-এ গিয়ে আপনার Linux ডিস্ট্রো (যেমন Ubuntu) অন করে দিন।

### ⚠️ ঘন ঘন হওয়া ভুল (Common Mistakes):

- **Virtualization Disable:** বায়োস (BIOS) থেকে 'Virtualization' অন না থাকলে Docker চলবে না। এরর আসলে চেক করুন CPU Virtualization 'Enabled' কি না।

---

## 🟡 Phase 3: একটি React (Vite) প্রজেক্টকে Dockerize করা

ধরা যাক আপনার একটি Vite দিয়ে তৈরি React প্রজেক্ট আছে। এটাকে Docker-এ নিতে নিচের স্টেপগুলো ফলো করুন।

### ১. `.dockerignore` ফাইল তৈরি করুন

**কোথায় তৈরি করবেন?** আপনার প্রজেক্টের একদম প্রধান ফোল্ডারে (Root folder), যেখানে `package.json` ফাইলটি থাকে।
**ফাইলের নাম:** `.dockerignore` (নামের আগে অবশ্যই একটি ডট বা ফুল-স্টপ দেবেন)।

কেন? কারণ আমরা চাই না `node_modules` বা লিন্ট ফাইলগুলো ইমেজের ভেতরে গিয়ে সাইজ বড় করুক।

```text
node_modules
dist
.git
Dockerfile
.dockerignore
```

### ২. `Dockerfile` তৈরি করুন

**কোথায় তৈরি করবেন?** প্রজেক্টের রুট ফোল্ডারে (Root folder)।
**ফাইলের নাম:** `Dockerfile` (এটির কোনো এক্সটেনশন যেমন .txt বা .js হবে না, শুধু এই নামই)।

```dockerfile
# ১. বেজ ইমেজ হিসেবে Node ব্যবহার করব
# 'node:20-alpine' মানে হলো এটি একটি লিনাক্স (Alpine Linux) যার ভেতর Node.js আগে থেকেই ইনস্টল করা আছে।
# Alpine হলো লিনাক্সের একটি খুব ছোট বা 'mini' ভার্সন যাতে ইমেজের সাইজ অনেক কম থাকে।
FROM node:20-alpine

# ২. ডকার কন্টেইনারের ভেতরে কাজের ফোল্ডার সেট করা
WORKDIR /app

# ৩. আগে package.json কন্টেইনারে কপি করা (Caching সুবিধার জন্য)
COPY package.json .

# ৪. ডিপেনডেন্সি ইনস্টল করা
RUN npm install

# ৫. বাকি সব কোড কপি করা
COPY . .

# ৬. Vite এর ডিফল্ট পোর্ট ৫১৭৩ এক্সপোজ করা
EXPOSE 5173

# ৭. ডেভ সার্ভার রান করার কমান্ড
CMD ["npm", "run", "dev"]
```

### ৩. ইমেজ তৈরি করা (Build Image)

টার্মিনালে গিয়ে নিচের কমান্ডটি রান করুন:

```bash
docker build -t my-react-app .
```

- `-t`: মানে ট্যাগ বা নাম দেয়া।
- `.`: মানে বর্তমান ফোল্ডারের `Dockerfile` ব্যবহার করা।

### ৪. কন্টেইনার রান করা (Run Container)

```bash
docker run -d -p 5173:5173 --name my-app-container my-react-app
```

- `-d`: ডিটাচ মোড (পেছনে রান হবে)।
- `-p 5173:5173`: আপনার পিসির ৫১৭৩ পোর্টকে ডকারের ৫১৭৩ পোর্টের সাথে কানেক্ট করা।

---

---

## 🟠 Phase 4: Local Development-এ Volume এবং HMR (Hot Module Replacement)

প্রিভিয়াস স্টেপে আমরা ইমেজ বিল্ড করে রান করেছি। কিন্তু সমস্যা হলো, আমরা কোডে কোনো চেঞ্জ করলে সেটা কন্টেইনারে আপডেট হচ্ছে না। প্রতিবার ইমেজ রি-বিল্ড করা তো সম্ভব না! এখানেই আসে **Volume**।

### ১. Volume কি?

Volume হলো আপনার পিসির ফোল্ডারের সাথে ডকার কন্টেইনারের ফোল্ডারের একটি "লাইভ কানেকশন"। পিসিতে সেভ করলে অটোমেটিক কন্টেইনারে আপডেট হবে।

### ২. Vite HMR সমস্যা ও সমাধান (Windows Special)

Windows-এ ডকার ভলিউম ব্যবহার করলে অনেক সময় Vite কোড চেঞ্জ ডিটেক্ট করতে পারে না। এর জন্য `vite.config.js`-এ ছোট্ট একটি ট্রিক করতে হয়:

**`vite.config.js` আপডেট করুন:**

```javascript
export default defineConfig({
  // ... existing config
  server: {
    watch: {
      usePolling: true, // এটি উইন্ডোজের জন্য মাস্ট!
    },
    host: true, // কন্টেইনারের বাইরে থেকে এক্সেস পাওয়ার জন্য
    port: 5173,
  },
})
```

### ৩. কমান্ড দিয়ে রান করা:

```bash
docker run -d -p 5173:5173 --name my-app -v ${PWD}:/app -v /app/node_modules my-react-app
```

- `-v ${PWD}:/app`: বর্তমান ডিরেক্টরিকে কন্টেইনারের `/app` এর সাথে লিঙ্কেড করা।
- `-v /app/node_modules`: কন্টেইনারের ভেতরের `node_modules` যেন পিসিরটা দিয়ে রিপ্লেস না হয় (এটি সিকিউরিটি হিসেবে কাজ করে)।

---

## 🟣 Phase 5: Docker Compose (এক কমান্ডে সবকিছু)

এতো বড় কমান্ড বারবার মনে রাখা কঠিন। তাই আমরা ব্যবহার করব `docker-compose.yml` ফাইল। এটি আপনার ডকার কনফিগারেশনকে একটি ফাইলে গুছিয়ে রাখে।

**কোথায় তৈরি করবেন?** প্রজেক্টের রুট ফোল্ডারে (যেখানে `Dockerfile` আছে)।
**ফাইলের নাম:** `docker-compose.yml`

```yaml
version: '3.8'
services:
  web:
    build: .
    ports:
      - '5173:5173'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true # Vite/HMR এর জন্য আরেকটি ব্যাকআপ অপশন
```

**কিভাবে রান করবেন?**

- রান করতে: `docker-compose up`
- স্টপ করতে: `docker-compose down`

---

## 🟤 Phase 6: Server Deployment (মৌলিক ধারণা)

সার্ভারে (যেমন AWS বা DigitalOcean) লোকাল ডেভেলপমেন্টের মতো `CMD ["npm", "run", "dev"]` ব্যবহার করা হয় না। সেখানে প্রজেক্ট **Build** করে Nginx বা অন্য কোনো ওয়েব সার্ভার দিয়ে চালানো হয়।

**সার্ভারের জন্য Dockerfile (Multi-stage build):**

```dockerfile
# Stage 1: Build
FROM node:20-alpine as build-stage
WORKDIR /app
COPY package.json .
RUN npm install
COPY . .
RUN npm run build

# Stage 2: Serve
FROM nginx:stable-alpine
COPY --from=build-stage /app/dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

---

## 🔴 Phase 7: ঘন ঘন হওয়া ভুল এবং টিপস (Common Mistakes)

১. **Port Mapping ভুল করা:** `docker run -p 3000:5173` দিলে আপনার ব্রাউজারে `localhost:3000` এ প্রজেক্ট ওপেন হবে, `5173` এ নয়। বাম পাশের সংখ্যাটি আপনার পিসির পোর্ট।
২. **Cache সমস্যা:** নতুন কোনো প্যাকেজ (`npm install`) দিলে ইমেজ আবার বিল্ড করতে হবে। ডকার অনেক সময় পুরোনো ইমেজ ক্যাশ ধরে রাখে। সেক্ষেত্রে `docker build --no-cache -t name .` দিতে পারেন।
৩. **ডকার কন্টেইনার ডিলিট না হওয়া:** নতুন চেঞ্জ রান করার আগে পুরোনো কন্টেইনার স্টপ ও ডিলিট করে নিন: `docker rm -f my-app-container`
৪. **Disk Space:** ডকার ইমেজ অনেক জায়গা নেয়। অপ্রয়োজনীয় ইমেজ ডিলিট করতে নিয়মিত `docker system prune` কমান্ডটি দিন।

---

## 🏆 কনক্লুশন: সফল ডকার ইউজার হওয়ার মন্ত্র

- সব ফাইল (`Dockerfile`, `.dockerignore`, `docker-compose.yml`) সরাসরি Root-এ রাখুন।
- উইন্ডোজে থাকলে `usePolling` ভুলবেন না।
- এরর আসলে প্রথমে `docker logs <container_id>` দিয়ে চেক করুন।

**শুভ ডকার জার্নি!** 🚀
