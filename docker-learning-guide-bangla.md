# 🐳 Docker শেখার সম্পূর্ণ গাইড (Bangla | Windows & Ubuntu)

> **এই গাইড কাদের জন্য:** যারা Docker বারবার শিখতে গিয়ে হাল ছেড়ে দিয়েছে। এখানে ধাপে ধাপে, সহজ ভাষায়, শুধু দরকারি জিনিস শেখাবো।
> **Platform:** Windows 10/11 & Ubuntu 24.04 LTS | **Use Case:** React (Vite) + যেকোনো Project

---

## 📋 গাইডের Phase সমূহ

| Phase | বিষয়                                 | কখন দরকার                          |
| :---- | :------------------------------------ | :--------------------------------- |
| 1     | Docker কেন দরকার?                     | শুরুতে একবার পড়ো                  |
| 2     | Docker Install (Windows & Ubuntu)     | শুরুতে একবার করো                   |
| 3     | হাতে-কলমে প্রথম Docker ব্যবহার        | শুরুতে একবার করো                   |
| 4     | নিজের Project Docker-এ চালানো         | প্রতি নতুন Project-এ একবার         |
| 5     | Docker Compose                        | প্রতি নতুন Project-এ একবার         |
| 6     | Daily Workflow — কখন কি করবে          | ⭐ প্রতিদিন (সবচেয়ে গুরুত্বপূর্ণ) |
| 7     | Production Build + AWS EC2 Deploy     | সার্ভারে দিতে গেলে                 |
| 8     | Newcomer Confusions + Troubleshooting | সমস্যা হলে দেখো                    |
| 9     | Command Cheatsheet                    | ভুলে গেলে দেখো                     |

---

---

# 🟢 PHASE 1: Docker কেন দরকার?

**🎯 উদ্দেশ্য:** Docker কি সেটা না, Docker **কেন লাগে** সেটা পরিষ্কার করা।

---

### সমস্যাটা কি?

তুমি একটা React project বানালে। তোমার পিসিতে সুন্দর চলছে। এখন—

```
😰 তোমার ল্যাপটপে চলছে, কিন্তু অফিসের পিসিতে চলছে না
😰 তোমার Node version 20, সার্ভারে Node 16 — Error আসছে
😰 তোমার কাছে Windows, সার্ভারে Linux — কিছু কাজ করছে না
😰 নতুন ডেভেলপার জয়েন করেছে, তার পিসিতে সেটআপ করতে ২ দিন লাগছে
```

### Docker কি করে?

Docker তোমার **পুরো environment-টা** প্যাক করে দেয় — কোড, Node version, সব dependency সহ।

```
তোমার পিসি     →  একটা প্যাকেট তৈরি হয় (Image)  →  যেকোনো জায়গায় চালাও
Windows            পুরো environment packed            Linux সার্ভার, Mac, অন্য পিসি
Node 20            Node 20 সহ                         ঠিক একইভাবে চলবে
```

### মনে রাখো

```
Docker ছাড়া  →  "আমার পিসিতে তো চলছিল!" 🤷
Docker দিয়ে  →  "যেখানেই চালাও, একইভাবে চলবে" ✅
```

---

### 🧠 Docker এর ৪টা মূল জিনিস (শুধু নাম জানো, বিস্তারিত পরে আসবে)

| জিনিস              | এক কথায়              | কোডে কি হয়                                                         |
| :----------------- | :-------------------- | :------------------------------------------------------------------ |
| **Dockerfile**     | রেসিপি (Instructions) | একটা টেক্সট ফাইল — কি install করবে, কি কপি করবে লেখা থাকে           |
| **Image**          | প্যাক করা বক্স        | `docker build` দিলে Dockerfile পড়ে তৈরি হয়। এটা শুধু থাকে, চলে না |
| **Container**      | চলমান অ্যাপ           | `docker run` দিলে Image থেকে তৈরি হয়। **এটাই আসলে চলে**            |
| **Docker Compose** | সব সেটিংস এক ফাইলে    | একটা `.yml` ফাইলে সব লিখে একটা কমান্ডে সব চালু                      |

```
Dockerfile লেখো → docker build → Image তৈরি → docker run → Container চলে
     📝                              📦                          🚀
```

---

---

# 🔵 PHASE 2: Docker Install (Windows & Ubuntu)

**🎯 উদ্দেশ্য:** তোমার পিসিতে Docker চালানোর জন্য সব সেটআপ করা। **এটা একবারই করতে হবে।**

---

> [!IMPORTANT]
> তুমি কোন অপারেটিং সিস্টেম ব্যবহার করছো? নিচে তোমারটার জন্য স্টেপগুলো ফলো করো:
>
> - **🪟 Windows ব্যবহারকারীরা:** "Windows Setup" সেকশনটি দেখো।
> - **🐧 Ubuntu ব্যবহারকারীরা:** "Ubuntu Setup" সেকশনটি দেখো।

---

## 🏁 Windows Setup (WSL2)

### Step 1: WSL2 Install করো

Docker Windows-এ সরাসরি চলে না। এটা ভেতরে একটা Mini Linux চালায়, যার নাম WSL (Windows Subsystem for Linux)।

**PowerShell Administrator হিসেবে ওপেন করো**, তারপর:

```powershell
wsl --install
```

| অংশ         | মানে                                                             |
| :---------- | :--------------------------------------------------------------- |
| `wsl`       | Windows Subsystem for Linux — Windows-এর ভেতরে Linux চালানোর টুল |
| `--install` | WSL + Ubuntu Linux দুটোই install করে দাও                         |

> ⚠️ কমান্ড দেওয়ার পর **কম্পিউটার Restart** দিতে হবে।

---

### Step 2: Docker Desktop Install করো

1. যাও → [docker.com/products/docker-desktop](https://www.docker.com/products/docker-desktop/)
2. **Download for Windows** → Install করো (Next, Next, Finish)
3. Install শেষে **Restart** দাও

---

### Step 3: Docker Desktop Settings Check

Docker Desktop ওপেন করো:

1. **⚙️ Settings → General** → ✅ `Use the WSL 2 based engine` চালু আছে কিনা দেখো
2. **Settings → Resources → WSL Integration** → তোমার Ubuntu distribution **Enable** করো

---

### Step 4: Verify — Docker কাজ করছে?

**PowerShell বা VS Code Terminal** ওপেন করো:

```bash
docker --version
```

```
✅ আউটপুট: Docker version 27.x.x, build xxxxxxx
```

```bash
docker run hello-world
```

| অংশ           | মানে                                                    |
| :------------ | :------------------------------------------------------ |
| `docker run`  | একটা Container চালাও                                    |
| `hello-world` | Docker Hub থেকে এই নামের ছোট্ট Image ডাউনলোড করে চালায় |

```
✅ আউটপুট: "Hello from Docker!" → মানে সব ঠিক আছে!
```

---

## 🐧 Ubuntu 24.04 LTS Setup (CLI)

উবুন্টুতে আমরা ডকার ইঞ্জিন (CLI) ব্যবহার করব কারণ এটি ফাস্ট এবং পিসির রিসোর্স কম খরচ করে।

**Step 1: পুরোনো ভার্সন রিমুভ (যদি থাকে)**

```bash
for pkg in docker.io docker-doc docker-compose docker-compose-v2 podman-docker containerd runc; do sudo apt-get remove $pkg; done
```

**Step 2: ডকার রিপোজিটরি সেটআপ**

```bash
# রিপোজিটরি অ্যাড করো
sudo apt-get update
sudo apt-get install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

# সোর্স লিস্টে ডকার অ্যাড করো
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu \
  $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt-get update
```

**Step 3: ডকার প্যাকেজ ইন্সটল**

```bash
sudo apt-get install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

**Step 4: Post-Installation (Sudo ছাড়া ডকার চালানো)**
উবুন্টুতে প্রতিবার `sudo` লিখতে না চাইলে নিচের কমান্ডগুলো দাও:

```bash
sudo usermod -aG docker $USER
# এরপর একবার লগআউট করে লগইন করো অথবা কম্পিউটার রিস্টার্ট দাও।
```

**Step 5: Verify**

```bash
docker --version
docker run hello-world
```

---

### ⚠️ Common Installation Errors

| সমস্যা                                            | সমাধান                                                       |
| :------------------------------------------------ | :----------------------------------------------------------- |
| "Hardware assisted virtualization is not enabled" | BIOS → Intel VT-x বা AMD-V **ON** করো, Restart দাও           |
| "WSL 2 is not installed"                          | PowerShell → `wsl --install` দাও, Restart দাও                |
| Docker Desktop ওপেন হচ্ছে না                      | Windows Update দাও, Docker Desktop Uninstall → Reinstall করো |

---

---

# 🟡 PHASE 3: হাতে-কলমে প্রথম Docker ব্যবহার

**🎯 উদ্দেশ্য:** Docker কিভাবে কাজ করে সেটা নিজের হাতে দেখা — কোনো Project ছাড়াই, শুধু Docker-এর basic কমান্ডগুলো শেখা।

---

### 🔹 3.1: Image ডাউনলোড করা (Pull)

Docker Hub হলো Image-এর online ভান্ডার (GitHub যেমন code-এর জন্য, Docker Hub তেমন Image-এর জন্য)।

```bash
docker pull nginx
```

| অংশ           | মানে                              |
| :------------ | :-------------------------------- |
| `docker pull` | Docker Hub থেকে Image ডাউনলোড করো |
| `nginx`       | Nginx নামের Web Server-এর Image   |

> 💡 `docker run` দিলে Image না থাকলে অটো pull হয়। তবে আলাদা pull করলে কি হচ্ছে সেটা স্পষ্ট বোঝা যায়।

---

### 🔹 3.2: Image দেখা

```bash
docker images
```

| অংশ             | মানে                               |
| :-------------- | :--------------------------------- |
| `docker images` | তোমার পিসিতে যত Image আছে সব দেখাও |

```
আউটপুট:
REPOSITORY    TAG       IMAGE ID       SIZE
nginx         latest    a1b2c3d4       187MB
hello-world   latest    e5f6g7h8       13.3kB
```

| Column     | মানে                           |
| :--------- | :----------------------------- |
| REPOSITORY | Image-এর নাম                   |
| TAG        | Version (না দিলে `latest` বসে) |
| IMAGE ID   | Image-এর unique ID             |
| SIZE       | Image-এর সাইজ                  |

---

### 🔹 3.3: Container চালানো (Run)

```bash
docker run -d -p 8080:80 --name my-nginx nginx
```

**প্রতিটা অংশ:**

| অংশ               | মানে                                                                    |
| :---------------- | :---------------------------------------------------------------------- |
| `docker run`      | Image থেকে Container তৈরি করো এবং চালু করো                              |
| `-d`              | **Detached mode** — Background-এ চলবে, Terminal আটকে রাখবে না           |
| `-p 8080:80`      | **Port mapping** — তোমার পিসির 8080 port → Container-এর 80 port-এ পাঠাও |
| `--name my-nginx` | Container-এর নাম দিলে `my-nginx` (না দিলে Docker random নাম দেয়)       |
| `nginx`           | কোন Image থেকে Container বানাবে                                         |

**Port mapping বোঝো:**

```
-p 8080:80
   ↑     ↑
   |     └── Container ভেতরের port (Nginx এখানে চলছে)
   └── তোমার পিসির port (Browser-এ এটা দিয়ে ঢুকবে)
```

এখন Browser-এ যাও → `http://localhost:8080` → Nginx-এর Welcome page দেখবে! 🎉

---

### 🔹 3.4: Container দেখা, বন্ধ করা, ডিলিট করা

```bash
# চলমান Container দেখো
docker ps
```

| অংশ         | মানে                                |
| :---------- | :---------------------------------- |
| `docker ps` | শুধু **চলমান** Container-গুলো দেখাও |

```bash
# সব Container দেখো (চলমান + বন্ধ)
docker ps -a
```

| অংশ  | মানে                                         |
| :--- | :------------------------------------------- |
| `-a` | **All** — বন্ধ হয়ে যাওয়া Container-ও দেখাও |

```bash
# Container বন্ধ করো
docker stop my-nginx
```

```bash
# Container ডিলিট করো (আগে বন্ধ করতে হবে)
docker rm my-nginx
```

```bash
# জোর করে ডিলিট (বন্ধ + ডিলিট একসাথে)
docker rm -f my-nginx
```

| অংশ           | মানে                                       |
| :------------ | :----------------------------------------- |
| `docker stop` | Container বন্ধ করো (ডাটা থাকে)             |
| `docker rm`   | Container ডিলিট করো (সব শেষ)               |
| `-f`          | **Force** — চলমান থাকলেও জোর করে ডিলিট করো |

---

### 🔹 3.5: Container-এর ভেতরে ঢোকা

```bash
docker exec -it my-nginx sh
```

| অংশ           | মানে                                                                  |
| :------------ | :-------------------------------------------------------------------- |
| `docker exec` | চলমান Container-এর ভেতরে কমান্ড চালাও                                 |
| `-it`         | **Interactive Terminal** — তোমাকে ভেতরে ঢুকতে দাও (keyboard input সহ) |
| `my-nginx`    | কোন Container-এ ঢুকবে                                                 |
| `sh`          | Shell ওপেন করো (Linux terminal)                                       |

> বের হতে `exit` লিখে Enter দাও।

> 💡 এটা Debugging-এ কাজে লাগে — Container ভেতরে ফাইল আছে কিনা, environment variable ঠিক আছে কিনা চেক করতে পারো।

---

### 🔹 3.6: Log দেখা

```bash
docker logs my-nginx
```

| অংশ           | মানে                                                                         |
| :------------ | :--------------------------------------------------------------------------- |
| `docker logs` | Container-এর console output দেখাও (যেমন তোমার terminal-এ `console.log` দেখো) |

```bash
# রিয়েলটাইম log — নতুন log আসলে অটো দেখাবে
docker logs -f my-nginx
```

| অংশ  | মানে                                                 |
| :--- | :--------------------------------------------------- |
| `-f` | **Follow** — log stream করো, নতুন log আসলে অটো দেখাও |

> বন্ধ করতে `Ctrl + C` চাপো।

---

### 🔹 3.7: Image ডিলিট করা

```bash
# একটা Image ডিলিট
docker rmi nginx
```

| অংশ          | মানে                               |
| :----------- | :--------------------------------- |
| `docker rmi` | **Remove Image** — Image ডিলিট করো |

> ⚠️ Image ডিলিট করতে হলে সেই Image থেকে বানানো সব Container আগে ডিলিট করতে হবে।

---

### 🔹 3.8: পরিষ্কার করা (Cleanup)

```bash
# সব অব্যবহৃত Image, Container, Volume ডিলিট করো
docker system prune -a
```

| অংশ                   | মানে                                                     |
| :-------------------- | :------------------------------------------------------- |
| `docker system prune` | অব্যবহৃত Docker resources পরিষ্কার করো                   |
| `-a`                  | **All** — সব unused Image-ও ডিলিট করো (শুধু dangling না) |

> ⚠️ চলমান Container-এর কিছু Delete হবে না। শুধু অব্যবহৃত জিনিস পরিষ্কার হবে।

---

### ✅ Phase 3 Summary: কি কি শিখলে

```
docker pull <image>                    → Image ডাউনলোড
docker images                          → Image list দেখো
docker run -d -p <pc>:<container> ...  → Container চালাও
docker ps / docker ps -a               → Container list দেখো
docker stop <name>                     → বন্ধ করো
docker rm <name>                       → ডিলিট করো
docker exec -it <name> sh              → ভেতরে ঢোকো
docker logs <name>                     → Log দেখো
docker rmi <image>                     → Image ডিলিট
docker system prune -a                 → পরিষ্কার করো
```

> 🧹 **এখন পরিষ্কার করো:** পরের Phase-এ যাওয়ার আগে practice-এ তৈরি Container/Image ডিলিট করো:
>
> ```bash
> docker rm -f my-nginx
> docker system prune -a
> ```

---

---

# 🟠 PHASE 4: নিজের Project Docker-এ চালানো

**🎯 উদ্দেশ্য:** তোমার নিজের Project-এর জন্য Dockerfile লেখা, Image তৈরি করা, এবং Container-এ চালানো শেখা।

> এখানে React (Vite) দিয়ে দেখাচ্ছি, তবে শেষে অন্য Project-এর জন্যও (Node.js backend, Python ইত্যাদি) কিভাবে করবে সেটা দেখাবো।

---

### 📂 ধরো তোমার React Project এরকম:

```
D:\projects\my-react-app\
├── node_modules\
├── public\
├── src\
│   ├── App.jsx
│   └── main.jsx
├── index.html
├── package.json
├── vite.config.js
└── ...
```

**আমরা ২টা নতুন ফাইল তৈরি করবো:** `Dockerfile` এবং `.dockerignore`

---

### 🔹 4.1: `.dockerignore` তৈরি করো

**📍 কোথায়:** Project-এর Root folder-এ (যেখানে `package.json` আছে)
**📄 নাম:** `.dockerignore` (শুরুতে dot `.` আছে)

```text
node_modules
dist
.git
Dockerfile
.dockerignore
docker-compose.yml
.env
.env.local
```

**এটা কি করে?**

`.gitignore` যেমন Git-কে বলে "এগুলো ignore করো", `.dockerignore` ঠিক তেমনি Docker-কে বলে **"এই ফাইলগুলো Image-এর ভেতরে কপি কোরো না"**।

| Ignore করা ফাইল | কেন ignore করবে                                                              |
| :-------------- | :--------------------------------------------------------------------------- |
| `node_modules`  | Container ভেতরে আবার `npm install` হবে, তোমার পিসির `node_modules` দরকার নেই |
| `dist`          | Container ভেতরে আবার build হবে                                               |
| `.git`          | Git history Container-এ দরকার নেই, সাইজ বাড়ায়                              |
| `.env`          | Secret keys — Image-এ রাখা Security risk                                     |

---

### 🔹 4.2: `Dockerfile` তৈরি করো

**📍 কোথায়:** Project-এর Root folder-এ
**📄 নাম:** `Dockerfile` (কোনো extension নেই — `.txt` বা `.js` লাগাবে না)

```dockerfile
# 👉 Base image: Node.js 20 সহ একটা ছোট Linux environment
# "alpine" মানে অনেক ছোট সাইজের Linux (~150MB, normal হলে ~1GB)
FROM node:20-alpine

# 👉 Container ভেতরে /app ফোল্ডার তৈরি করো এবং সেখানে যাও
# তোমার পিসিতে cd /app দেওয়ার মতো
WORKDIR /app

# 👉 শুধু package.json আগে কপি করো
# কেন আলাদা? Docker caching — package.json না বদলালে npm install skip হবে (সময় বাঁচে!)
COPY package.json .

# 👉 Dependencies install করো (Container ভেতরে)
RUN npm install

# 👉 বাকি সব কোড কপি করো
# .dockerignore তে থাকা ফাইলগুলো বাদ যাবে
COPY . .

# 👉 Vite ডিফল্ট port 5173 — বাইরে থেকে connect করার জন্য expose করো
# Note: এটা শুধু documentation, আসল connection -p flag দিয়ে হয়
EXPOSE 5173

# 👉 Container চালু হলে এই command অটো চলবে
CMD ["npm", "run", "dev"]
```

**Dockerfile-এ ব্যবহৃত প্রতিটা Instruction:**

| Instruction | কি করে                                                       |
| :---------- | :----------------------------------------------------------- |
| `FROM`      | কোন Base Image ব্যবহার করবে (OS + Runtime)                   |
| `WORKDIR`   | Container ভেতরে কোন folder-এ কাজ করবে                        |
| `COPY`      | তোমার পিসি থেকে Container-এ ফাইল কপি করো                     |
| `RUN`       | Image তৈরির সময় একটা command চালাও (install, build ইত্যাদি) |
| `EXPOSE`    | কোন port-এ app চলবে সেটা document করো                        |
| `CMD`       | Container চালু হলে কোন command চলবে                          |

> 💡 **`RUN` vs `CMD` পার্থক্য:**
>
> - `RUN` → Image **তৈরির সময়** চলে (build time) — যেমন `npm install`
> - `CMD` → Container **চালু হওয়ার সময়** চলে (run time) — যেমন `npm run dev`

---

### 🔹 4.3: `vite.config.js` Update করো (React Vite Project-এর জন্য)

**কেন?** Docker Container একটা আলাদা environment। সেখান থেকে তোমার browser-এ access পেতে হলে কিছু settings দরকার।

> **💡 উবুন্টু (Ubuntu) ইউজারদের জন্য:** উবুন্টুতে ফাইল সিন্কিং নিয়ে সমস্যা হয় না, তাই `usePolling: true` অপশনটি ছাড়াই আপনার কোড আপডেট হবে। এতে কম্পিউটারের পাওয়ার সেভ হবে।

```javascript
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  server: {
    // Container-এর বাইরে থেকে access পাবে (ছাড়া দিলে browser-এ কিছু দেখাবে না)
    host: true,
    // Port fix করো
    port: 5173,
    // 🪟 Windows (WSL2) + Docker-এ file change detect করতে polling লাগে।
    // 🐧 Ubuntu তে নিচের অংশটুকু দরকার নেই (watch block বাদ দিতে পারো)।
    watch: {
      usePolling: true,
    },
  },
});
```

| Setting            | কেন দরকার                                                                           |
| :----------------- | :---------------------------------------------------------------------------------- |
| `host: true`       | ছাড়া দিলে Container চলবে কিন্তু browser-এ কিছু আসবে না                             |
| `port: 5173`       | Port fix করা — যাতে port mapping ঠিক কাজ করে                                        |
| `usePolling: true` | Windows-এ Docker volume ব্যবহার করলে Vite file change ধরতে পারে না, polling fix করে |

> ⚠️ এই settings **শুধু React (Vite) project-এর জন্য**। অন্য project-এ দরকার নেই।

---

### 🔹 4.4: Image Build এবং Container Run

```bash
# Step 1: Image তৈরি করো
docker build -t my-react-app .
```

| অংশ               | মানে                                      |
| :---------------- | :---------------------------------------- |
| `docker build`    | Dockerfile পড়ে Image তৈরি করো            |
| `-t my-react-app` | **Tag** — Image-এর নাম দাও `my-react-app` |
| `.`               | **বর্তমান folder-এ** Dockerfile খোঁজো     |

```bash
# Step 2: Container চালু করো
docker run -d -p 5173:5173 --name react-dev my-react-app
```

| অংশ                | মানে                             |
| :----------------- | :------------------------------- |
| `docker run`       | Image থেকে Container তৈরি + চালু |
| `-d`               | Background-এ চলবে                |
| `-p 5173:5173`     | পিসির 5173 → Container-এর 5173   |
| `--name react-dev` | Container-এর নাম                 |
| `my-react-app`     | কোন Image ব্যবহার করবে           |

Browser → `http://localhost:5173` → তোমার React app দেখবে! 🎉

---

### 🔹 4.5: অন্য Project-এর Dockerfile কেমন হয়?

#### Node.js (Express) Backend:

```dockerfile
FROM node:20-alpine
WORKDIR /app
COPY package.json .
RUN npm install
COPY . .
EXPOSE 3000
CMD ["node", "server.js"]
```

#### Python (Flask/Django):

```dockerfile
FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
EXPOSE 5000
CMD ["python", "app.py"]
```

> 💡 **Pattern দেখো — সব Dockerfile-এর structure প্রায় একই:**
> `FROM` → `WORKDIR` → dependency file `COPY` → dependency `install` → বাকি কোড `COPY` → `EXPOSE` → `CMD`

---

---

# 🟣 PHASE 5: Docker Compose — এক ফাইলে সব সেটিংস

**🎯 উদ্দেশ্য:** প্রতিবার লম্বা `docker build` + `docker run -d -p ... -v ...` কমান্ড না লিখে, সব সেটিংস একটা ফাইলে রাখা। **একটা কমান্ডে সব চালু, একটা কমান্ডে সব বন্ধ।**

---

### 🤔 Compose কেন দরকার?

**Compose ছাড়া** প্রতিবার এই লম্বা কমান্ড লিখতে হতো:

```bash
docker build -t my-react-app .
docker run -d -p 5173:5173 --name react-dev -v ${PWD}:/app -v /app/node_modules my-react-app
```

**Compose দিয়ে** শুধু:

```bash
docker compose up -d
```

**ব্যস! Build + Run + Volume + Port — সব অটো।**

---

### 🔹 5.1: `docker-compose.yml` তৈরি করো

**📍 কোথায়:** Project-এর Root folder-এ (যেখানে `Dockerfile` আছে)
**📄 নাম:** `docker-compose.yml`

```yaml
services:
  # "app" হলো সার্ভিসের নাম — যেকোনো নাম দিতে পারো
  app:
    # বর্তমান folder-এর Dockerfile দিয়ে Image build করো
    build: .

    # Port mapping
    ports:
      - "5173:5173"

    # Volume: তোমার পিসির code আর Container-এর code sync রাখো
    volumes:
      # পিসির current folder (./) → Container-এর /app folder-এ link
      # ফলে পিসিতে code change করলে Container-এ অটো reflect হবে
      - ./:/app

      # Container-এর node_modules আলাদা রাখো (পিসির সাথে mix করো না)
      # কেন? পিসি Windows, Container Linux — দুটোর node_modules আলাদা
      - /app/node_modules

    # Environment variable
    environment:
      # 🪟 Windows (WSL2) এর জন্য এটি জরুরি।
      # 🐧 Ubuntu তে এটি না দিলেও চলবে।
      - CHOKIDAR_USEPOLLING=true
```

**yml ফাইলের প্রতিটি অংশ:**

| অংশ                          | কি করে                            | `docker run` এ যেটার সমান     |
| :--------------------------- | :-------------------------------- | :---------------------------- |
| `build: .`                   | Dockerfile দিয়ে Image build      | `docker build -t ... .`       |
| `ports: "5173:5173"`         | Port mapping                      | `-p 5173:5173`                |
| `volumes: ./:/app`           | পিসির folder → Container-এ link   | `-v ${PWD}:/app`              |
| `volumes: /app/node_modules` | Container-এর node_modules protect | `-v /app/node_modules`        |
| `environment:`               | Environment variable set          | `-e CHOKIDAR_USEPOLLING=true` |

---

### 🔹 5.2: Volume কি? কেন লাগে?

**Volume ছাড়া:**

```
তুমি পিসিতে App.jsx চেঞ্জ করলে → Container-এ কিছু হবে না 😢
কারণ code Image build-এর সময় কপি হয়ে গেছে — পিসি আর Container আলাদা
```

**Volume দিলে:**

```
তুমি পিসিতে App.jsx চেঞ্জ করলে → Container-এ অটো আপডেট হবে 🎉
Volume পিসি আর Container-কে live sync রাখে
```

> 💡 Volume = Google Drive Sync-এর মতো। পিসিতে file change → Container-এও change।

**দুটো Volume কেন?**

```yaml
volumes:
  - ./:/app # ① পিসির code → Container-এ sync করো
  - /app/node_modules # ② কিন্তু node_modules sync কোরো না!
```

| Volume              | কেন                                                                           |
| :------------------ | :---------------------------------------------------------------------------- |
| `./:/app`           | কোড sync — live reload কাজ করবে                                               |
| `/app/node_modules` | পিসি Windows, Container Linux — দুটোর `node_modules` আলাদা। Mix হলে error হবে |

---

### 🔹 5.3: docker-compose.yml — কোথায় রাখবে?

**Scenario 1: শুধু Frontend (বা শুধু যেকোনো একটা project)**

```
my-react-app/
├── docker-compose.yml    ← Dockerfile-এর পাশে
├── Dockerfile
├── package.json
└── src/
```

**Scenario 2: Frontend + Backend আলাদা folder-এ**

```
my-fullstack-app/
├── docker-compose.yml    ← বাইরে, দুটো folder-এর parent-এ
├── frontend/
│   ├── Dockerfile
│   └── package.json
└── backend/
    ├── Dockerfile
    └── package.json
```

Scenario 2 এর `docker-compose.yml`:

```yaml
services:
  frontend:
    build: ./frontend # frontend folder-এর Dockerfile ব্যবহার করো
    ports:
      - "5173:5173"

  backend:
    build: ./backend # backend folder-এর Dockerfile ব্যবহার করো
    ports:
      - "3000:3000"
```

> 💡 তুমি এখন শুধু Scenario 1 ফলো করো। পরে Backend যোগ করলে Scenario 2 তে যাবে।

---

### 🔹 5.4: Docker Compose Commands

```bash
# চালু করো (background-এ)
docker compose up -d
```

| অংশ                 | মানে                                                 |
| :------------------ | :--------------------------------------------------- |
| `docker compose up` | yml পড়ে Image build + Container চালু (দুটোই একসাথে) |
| `-d`                | Background-এ চালু করো                                |

```bash
# বন্ধ করো (Container ডিলিট হবে)
docker compose down
```

```bash
# Image নতুন করে build করে চালু করো
docker compose up --build -d
```

| অংশ       | মানে                                                |
| :-------- | :-------------------------------------------------- |
| `--build` | আগের cached Image ব্যবহার না করে নতুন করে build করো |

```bash
# Log দেখো (realtime)
docker compose logs -f
```

```bash
# Compose-এর Container-গুলো দেখো
docker compose ps
```

---

### ⚠️ Compose Confusion: `docker-compose` vs `docker compose`

```bash
# পুরোনো style (Docker Compose V1) — আলাদা tool ছিল
docker-compose up -d

# নতুন style (Docker Compose V2) — Docker-এর built-in ✅
docker compose up -d
```

**দুটোই একই কাজ করে।** তবে নতুন Docker-এ `docker compose` (hyphen ছাড়া) ব্যবহার করো। Internet-এ পুরোনো tutorial-এ `docker-compose` দেখলে ঘাবড়াবে না — একই জিনিস।

---

---

# ⭐ PHASE 6: Daily Workflow — কখন কি করবে

**🎯 উদ্দেশ্য:** এটাই সবচেয়ে গুরুত্বপূর্ণ Phase। Project setup (Phase 4-5) একবারই করবে। কিন্তু প্রতিদিন কোন command দেবে, কোড change করলে কি হবে, package install করলে কি করবে — সেটা এখানে শিখবে।

---

### 🔹 6.1: প্রতিদিনের Routine

**🌅 কাজ শুরু করার সময়:**

```bash
# 1. Docker Desktop চালু করো (tray icon দেখলে মানে চালু আছে)
# 2. Terminal-এ project folder-এ যাও
# 3. চালু করো:
docker compose up -d

> **💡 উবুন্টু (Ubuntu) ইউজারদের জন্য:** আপনি যদি Step 4 (Post-Installation) মিস করে থাকেন, তবে প্রতিবার কমান্ডের আগে `sudo` দিতে হতে পারে (উদা: `sudo docker compose up -d`)।
```

Browser → `http://localhost:5173` → কোডিং শুরু! 🎉

**🌙 কাজ শেষে:**

```bash
docker compose down
```

**ব্যস! প্রতিদিন এই ২টা command-ই লাগবে।**

---

### 🔹 6.2: Decision Tree — কোন পরিস্থিতিতে কি করবে

```
তুমি কি করেছো?
│
├── 🔵 শুধু Code change (JSX, CSS, JS ইত্যাদি)
│   └── ✅ কিছু করার দরকার নেই!
│       Volume + Vite HMR → Browser অটো refresh হবে
│
├── 🟡 নতুন npm package install করতে চাও
│   └── package.json-এ add করো, তারপর:
│       docker compose down
│       docker compose up --build -d
│
├── 🟠 Dockerfile বা docker-compose.yml change করেছো
│   └── docker compose down
│       docker compose up --build -d
│
├── 🔴 কিছু কাজ করছে না / Error আসছে
│   └── docker compose logs -f     ← আগে log দেখো
│       তারপরও সমস্যা থাকলে:
│       docker compose down
│       docker compose up --build --force-recreate -d
│
└── 🟣 সব ঝামেলা clear করে নতুন করে শুরু করতে চাও
    └── docker compose down
        docker system prune -a
        docker compose up --build -d
```

---

### 🔹 6.3: প্রতিটি Scenario বিস্তারিত

#### 🔵 Scenario: শুধু Code Change (সবচেয়ে common)

```
তুমি VS Code-এ App.jsx change করে Save দিলে
        ↓
Volume sync → Container-এ অটো update
        ↓
Vite HMR → Browser অটো refresh
        ↓
✅ কোনো Docker command লাগবে না!
```

> এটাই Docker-এর সৌন্দর্য — তুমি আগের মতোই code করবে, Docker background-এ চলছে।

---

#### 🟡 Scenario: নতুন npm Package Install

**ভুল উপায়:**

```bash
# ❌ পিসিতে সরাসরি npm install — Container-এ apply হবে না
npm install axios
```

**সঠিক উপায়:**

```bash
# Step 1: পিসিতে package.json-এ package add করো
npm install axios        # এটা শুধু package.json update করতে দিচ্ছো

# Step 2: Container rebuild করো
docker compose down
docker compose up --build -d
```

**কেন rebuild লাগে?** `package.json` change হয়েছে → নতুন Image build করতে হবে যেখানে নতুন package install থাকবে।

> 💡 **Alternative:** Container ভেতরে সরাসরি install করতে পারো (temporary):
>
> ```bash
> docker compose exec app npm install axios
> ```
>
> তবে এটা temporary — container recreate করলে হারিয়ে যাবে। তাই `--build` approach-ই standard।

---

#### 🟠 Scenario: Dockerfile বা docker-compose.yml Change

```bash
docker compose down
docker compose up --build -d
```

যেকোনো Docker config file change করলে rebuild দরকার।

---

#### 🔴 Scenario: কিছু কাজ করছে না

```bash
# Step 1: আগে log দেখো — কোথায় error সেটা বুঝো
docker compose logs -f

# Step 2: Rebuild + Force recreate
docker compose down
docker compose up --build --force-recreate -d
```

| Flag               | মানে                                              |
| :----------------- | :------------------------------------------------ |
| `--build`          | Image নতুন করে build                              |
| `--force-recreate` | Container পুরো নতুন করে তৈরি (পুরোনো state clear) |

---

#### 🟣 Scenario: সব Clean করে Fresh Start

```bash
docker compose down
docker system prune -a        # সব unused image/container delete
docker compose up --build -d  # পুরো নতুন করে শুরু
```

---

### 🔹 6.4: সংক্ষেপে মনে রাখো

| পরিস্থিতি               | কি করবে                                                |
| :---------------------- | :----------------------------------------------------- |
| সকালে কাজ শুরু          | `docker compose up -d`                                 |
| Code change করলে        | ❌ কিছু না — অটো হবে                                   |
| Package install করলে    | `docker compose down` → `docker compose up --build -d` |
| Config file change করলে | `docker compose down` → `docker compose up --build -d` |
| Error আসলে              | `docker compose logs -f` দিয়ে দেখো                    |
| কাজ শেষে                | `docker compose down`                                  |

---

---

# 🔴 PHASE 7: Production Build + AWS EC2 Deploy

**🎯 উদ্দেশ্য:** লোকাল development-এ আমরা `npm run dev` দিয়ে চালাই। কিন্তু সার্ভারে real user-দের জন্য Production build দিতে হয়। এই Phase-এ শিখবে কিভাবে Production-ready Image তৈরি করে AWS EC2 তে deploy করবে।

---

### 🔹 7.1: Development vs Production পার্থক্য

| বিষয়      | Development (লোকাল) | Production (সার্ভার)                |
| :--------- | :------------------ | :---------------------------------- |
| Command    | `npm run dev`       | `npm run build` → Nginx দিয়ে serve |
| Speed      | ধীর (Debug mode)    | Fast (Optimized, minified)          |
| Image size | ~250MB+ (Node সহ)   | ~30-50MB (শুধু Nginx + built files) |
| Hot Reload | আছে                 | নেই (দরকারও নেই)                    |
| Volume     | লাগে (code sync)    | লাগে না (code Image-এ baked)        |

---

### 🔹 7.2: Production Dockerfile তৈরি করো (Multi-stage Build)

**📍 কোথায়:** Project root-এ
**📄 নাম:** `Dockerfile.prod` (আলাদা নাম — dev Dockerfile-এর সাথে mix হবে না)

```dockerfile
# ==========================================
# Stage 1: BUILD — React app build করো
# ==========================================
FROM node:20-alpine AS build

WORKDIR /app
COPY package.json .
RUN npm install
COPY . .

# Production build → dist/ folder-এ optimized files তৈরি হবে
RUN npm run build


# ==========================================
# Stage 2: SERVE — শুধু built files serve করো
# ==========================================
# Nginx = lightweight web server, static files serve করতে best
FROM nginx:alpine

# Stage 1 এর dist/ folder → Nginx-এর serve folder-এ কপি
COPY --from=build /app/dist /usr/share/nginx/html

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

**নতুন concept — Multi-stage Build:**

| অংশ                 | মানে                                                    |
| :------------------ | :------------------------------------------------------ |
| `FROM ... AS build` | প্রথম stage-এর নাম দিলে `build`                         |
| `RUN npm run build` | React build → `dist/` folder-এ ছোট optimized files তৈরি |
| `FROM nginx:alpine` | দ্বিতীয় stage শুরু — এখন শুধু Nginx, Node.js নেই       |
| `COPY --from=build` | প্রথম stage (`build`) থেকে শুধু `dist/` folder নাও      |

**কেন Multi-stage?**

```
❌ Single stage → Image = Node + node_modules + code + dist = 500MB+
✅ Multi-stage → Image = শুধু Nginx + dist (HTML/CSS/JS) = 30-50MB

কারণ Stage 2 তে Node.js, node_modules কিছুই নেই — শুধু ready files!
```

---

### 🔹 7.3: Local-এ Production Build Test

```bash
# Production Image build করো (-f দিয়ে কোন Dockerfile বলে দিচ্ছো)
docker build -f Dockerfile.prod -t my-react-app-prod .
```

| অংশ                    | মানে                                                                                |
| :--------------------- | :---------------------------------------------------------------------------------- |
| `-f Dockerfile.prod`   | **File** — কোন Dockerfile ব্যবহার করবে (default `Dockerfile` না, `Dockerfile.prod`) |
| `-t my-react-app-prod` | Image-এর নাম                                                                        |

```bash
# Container চালু করো
docker run -d -p 80:80 --name react-prod my-react-app-prod
```

Browser → `http://localhost` → Production version দেখবে! 🎉

> 💡 Port 80 হলো HTTP-এর default port, তাই `:80` না লিখলেও চলে।

Test শেষে clean up:

```bash
docker rm -f react-prod
```

---

### 🔹 7.4: Docker Hub-এ Image Push করো

AWS EC2 তে Image পাঠানোর সবচেয়ে সহজ উপায় — Docker Hub-এ upload করা, তারপর সার্ভার থেকে download।

**Step 1: Docker Hub Account তৈরি করো**
→ [hub.docker.com](https://hub.docker.com/) → Sign Up (Free)

**Step 2: Terminal-এ Docker Hub-এ login করো**

```bash
docker login
```

Username আর Password দাও।

**Step 3: Image-কে তোমার Docker Hub username দিয়ে tag করো**

```bash
docker tag my-react-app-prod yourusername/my-react-app-prod:v1
```

| অংশ                                 | মানে                                             |
| :---------------------------------- | :----------------------------------------------- |
| `docker tag`                        | Image-এর নতুন নাম দাও                            |
| `my-react-app-prod`                 | Local image-এর বর্তমান নাম                       |
| `yourusername/my-react-app-prod:v1` | Docker Hub format: `username/image-name:version` |

**Step 4: Push করো**

```bash
docker push yourusername/my-react-app-prod:v1
```

| অংশ           | মানে                          |
| :------------ | :---------------------------- |
| `docker push` | Docker Hub-এ Image upload করো |

---

### 🔹 7.5: AWS EC2 তে Deploy

**ধরে নিচ্ছি:** তোমার একটা EC2 instance আছে (Ubuntu) এবং তুমি SSH দিয়ে connect করতে পারো।

**Step 1: EC2 তে SSH connect করো (তোমার পিসি থেকে)**

```bash
ssh -i "your-key.pem" ubuntu@your-ec2-ip
```

**Step 2: EC2 তে Docker install করো (প্রথমবার একবারই)**

```bash
# Package update
sudo apt update

# Docker install
sudo apt install -y docker.io docker-compose-plugin

# Docker কে sudo ছাড়া চালানোর permission দাও
sudo usermod -aG docker $USER

# Logout করে আবার login করো (permission apply হতে)
exit
```

আবার SSH connect করো।

**Step 3: Docker Hub থেকে Image pull করো**

```bash
docker pull yourusername/my-react-app-prod:v1
```

**Step 4: Container চালু করো**

```bash
docker run -d -p 80:80 --name my-website yourusername/my-react-app-prod:v1
```

Browser → `http://your-ec2-ip` → তোমার website live! 🎉

---

### 🔹 7.6: Update Deploy করার সময় কি করবে

তোমার পিসিতে:

```bash
# 1. নতুন Production Image build
docker build -f Dockerfile.prod -t my-react-app-prod .

# 2. নতুন version tag দাও
docker tag my-react-app-prod yourusername/my-react-app-prod:v2

# 3. Push
docker push yourusername/my-react-app-prod:v2
```

EC2 সার্ভারে (SSH দিয়ে connect করে):

```bash
# 1. পুরোনো container বন্ধ + delete
docker rm -f my-website

# 2. নতুন version pull
docker pull yourusername/my-react-app-prod:v2

# 3. নতুন container চালু
docker run -d -p 80:80 --name my-website yourusername/my-react-app-prod:v2
```

---

### 🔹 7.7: Alternative — Docker Hub ছাড়া Deploy (docker-compose দিয়ে সার্ভারে build)

Docker Hub ব্যবহার না করতে চাইলে, সার্ভারে সরাসরি code নিয়ে build করতে পারো:

EC2 সার্ভারে:

```bash
# 1. Git থেকে code pull
git clone https://github.com/yourusername/my-react-app.git
cd my-react-app

# 2. Production image build + run
docker build -f Dockerfile.prod -t my-react-app-prod .
docker run -d -p 80:80 --name my-website my-react-app-prod
```

> 💡 **কোনটা ভালো?**
>
> - **Docker Hub:** Image ready বানিয়ে push করো → সার্ভারে শুধু pull + run (faster deploy)
> - **সার্ভারে build:** Code push → সার্ভারে build (সহজ workflow, কিন্তু সার্ভারে build time লাগে)
> - ছোট project-এ দুটোই ঠিক আছে। বড় project-এ Docker Hub approach better।

---

---

# 🧩 PHASE 8: Newcomer Confusions + Troubleshooting

**🎯 উদ্দেশ্য:** Docker শিখতে গিয়ে যে confusion এবং সমস্যাগুলো সবচেয়ে বেশি হয়, সেগুলো এক জায়গায়।

---

### 🔹 8.1: একই কাজ — ভিন্ন ভিন্ন Command দেখো কেন?

#### Confusion 1: `docker run` — কখন Volume সহ, কখন ছাড়া?

Internet-এ দুইভাবে দেখবে:

```bash
# Version A: Volume ছাড়া
docker run -d -p 5173:5173 --name react-dev my-react-app

# Version B: Volume সহ
docker run -d -p 5173:5173 --name react-dev -v ${PWD}:/app -v /app/node_modules my-react-app
```

**পার্থক্য:**

|                  | Volume ছাড়া (Version A)            | Volume সহ (Version B)       |
| :--------------- | :---------------------------------- | :-------------------------- |
| Code change করলে | ❌ Container-এ reflect হবে না       | ✅ অটো reflect হবে          |
| কখন ব্যবহার      | Production-এ, বা one-time run       | Development-এ (live coding) |
| মূল কথা          | Code Image-এ baked, পরিবর্তন হয় না | পিসি ↔ Container live sync  |

**Rule:**

```
Development → Volume দাও (code change দেখতে চাও)
Production  → Volume দিও না (code fixed, change হবে না)
```

> 💡 তুমি `docker-compose.yml` ব্যবহার করলে Volume সেখানেই লেখা থাকে, আলাদা মনে রাখতে হয় না!

---

#### Confusion 2: `docker compose up` vs `docker compose up --build`

```bash
# Version A: শুধু up
docker compose up -d

# Version B: build সহ up
docker compose up --build -d
```

**পার্থক্য:**

|                      | `up -d`                                 | `up --build -d`                        |
| :------------------- | :-------------------------------------- | :------------------------------------- |
| Image আগে থেকে থাকলে | পুরোনো Image ব্যবহার করবে               | নতুন করে build করবে                    |
| কখন ব্যবহার          | সকালে কাজ শুরু করতে (কিছু change হয়নি) | package.json বা Dockerfile change করলে |

**Rule:**

```
কিছু change করোনি     → docker compose up -d
Dockerfile/package.json change → docker compose up --build -d
বুঝতে পারছো না কি হচ্ছে   → docker compose up --build -d (safe choice)
```

---

#### Confusion 3: `docker compose down` vs `docker compose stop`

```bash
# Version A: down
docker compose down

# Version B: stop
docker compose stop
```

**পার্থক্য:**

|             | `down`                                  | `stop`                                    |
| :---------- | :-------------------------------------- | :---------------------------------------- |
| Container   | বন্ধ + **ডিলিট**                        | শুধু বন্ধ (থাকবে)                         |
| Network     | ডিলিট                                   | থাকবে                                     |
| আবার চালু   | `docker compose up -d` (নতুন container) | `docker compose start` (পুরোনো container) |
| কখন ব্যবহার | কাজ শেষে clean করতে চাও                 | একটু pause দিতে চাও                       |

**Rule:**

```
কাজ শেষ, computer বন্ধ করবো  → docker compose down
lunch break, একটু পরে আবার কাজ → docker compose stop, তারপর docker compose start
```

---

#### Confusion 4: `Dockerfile` vs `docker-compose.yml` — কোনটা কখন?

|            | Dockerfile                           | docker-compose.yml                                           |
| :--------- | :----------------------------------- | :----------------------------------------------------------- |
| কি করে     | **একটা** Image তৈরির recipe          | **পুরো environment-এর** settings (port, volume, network)     |
| কখন লেখো   | প্রতিটা project/service-এর জন্য একটা | পুরো project-এর জন্য একটা                                    |
| ছাড়া চলে? | ❌ Image তৈরি করতেই লাগে             | ✅ ছাড়া `docker run` দিয়ে manually চালানো যায়, তবে ঝামেলা |

**মনে রাখো:**

```
Dockerfile        → Image-এর recipe (কি install করবে, কি কপি করবে)
docker-compose.yml → সেই Image কিভাবে চালাবে (port, volume, environment)
```

---

#### Confusion 5: `COPY . .` কি তোমার পুরো PC copy করে?

**না!** `COPY . .` মানে:

```
COPY  .      .
      ↑      ↑
      |      └── Container-এর বর্তমান WORKDIR-এ paste করো
      └── তোমার project folder থেকে (যেখানে Dockerfile আছে) copy করো
```

- শুধু **project folder-এর** ফাইল copy হয়, পুরো PC না
- `.dockerignore`-এ যা আছে সেগুলো skip হয়

---

#### Confusion 6: Container বন্ধ করলে কি সব code মুছে যায়?

```
Container stop করলে  → ❌ কিছু মুছে না, শুধু pause
Container rm করলে    → Container-এর ভেতরের data মুছে যায়
                       কিন্তু তোমার পিসির code? → ✅ 100% safe, কিছু হয় না
Image delete করলেও  → তোমার পিসির code ✅ safe
```

> তোমার PC-র source code Docker-এর উপর depend করে না। Docker শুধু copy/sync করে ব্যবহার করে।

---

### 🔹 8.2: Common Errors + Solutions

#### Error 1: "Port is already in use"

```
Error: bind: address already in use
```

**কারণ:** আগের container বন্ধ না করে নতুন container একই port-এ চালু করেছো।

**Fix:**

```bash
# কে port ধরে আছে দেখো
docker ps

# সেই container বন্ধ + delete করো
docker rm -f <container-name>
```

---

#### Error 2: Browser-এ কিছু দেখাচ্ছে না / "This site can't be reached"

**Check list:**

```bash
# 1. Container চলছে কিনা দেখো
docker ps

# 2. Container-এর log দেখো
docker compose logs -f
```

**Common কারণ:**
| কারণ | Fix |
|:-----|:----|
| `vite.config.js`-এ `host: true` নেই | Add করো |
| Port mapping ভুল | `-p 5173:5173` ঠিক আছে কিনা দেখো |
| Container crash করেছে | `docker compose logs -f` দিয়ে error দেখো |

---

#### Error 3: Code change হচ্ছে কিন্তু browser-এ update হচ্ছে না

**Fix:**

1. `docker-compose.yml`-এ volume আছে কিনা check করো
2. `vite.config.js`-এ (Windows এর জন্য) `usePolling: true` আছে কিনা check করো
3. Rebuild করো:

```bash
docker compose down
docker compose up --build -d
```

---

#### Error 4: "npm install" Error / dependency issue

**Fix:** Cache clear করে fresh build:

```bash
docker compose down
docker compose build --no-cache
docker compose up -d
```

| অংশ          | মানে                                                |
| :----------- | :-------------------------------------------------- |
| `--no-cache` | পুরোনো cached layer ব্যবহার না করে পুরো fresh build |

---

#### Error 5: Disk space শেষ হয়ে যাচ্ছে

Docker images অনেক জায়গা নেয়। Clean up করো:

```bash
# কতটুকু জায়গা Docker নিচ্ছে দেখো
docker system df

# অব্যবহৃত সব delete করো
docker system prune -a
```

---

#### Error 6: Container চালু হয়ে সাথে সাথে বন্ধ হয়ে যাচ্ছে

```bash
# Log দেখো — কি error দিয়ে বন্ধ হচ্ছে
docker compose logs
```

**Common কারণ:**

- `package.json`-এ `"dev"` script নেই
- `Dockerfile`-এ CMD ভুল
- Code-এ syntax error

---

---

# 📋 PHASE 9: Command Cheatsheet

**🎯 উদ্দেশ্য:** সব দরকারি command এক জায়গায় — ভুলে গেলে এখানে দেখো।

---

### 🖼️ Image Commands

| Command                                       | কি করে                           |
| :-------------------------------------------- | :------------------------------- |
| `docker build -t <name> .`                    | Image তৈরি                       |
| `docker build -f Dockerfile.prod -t <name> .` | নির্দিষ্ট Dockerfile দিয়ে build |
| `docker images`                               | সব Image দেখো                    |
| `docker rmi <name>`                           | Image delete                     |
| `docker tag <old> <new>`                      | Image rename/tag                 |
| `docker push <name>`                          | Docker Hub-এ upload              |
| `docker pull <name>`                          | Docker Hub থেকে download         |

### 📦 Container Commands

| Command                                                   | কি করে               |
| :-------------------------------------------------------- | :------------------- |
| `docker run -d -p <pc>:<container> --name <name> <image>` | Container চালু       |
| `docker ps`                                               | চলমান container দেখো |
| `docker ps -a`                                            | সব container দেখো    |
| `docker stop <name>`                                      | বন্ধ করো             |
| `docker start <name>`                                     | আবার চালু করো        |
| `docker rm <name>`                                        | delete করো           |
| `docker rm -f <name>`                                     | force delete         |
| `docker logs <name>`                                      | Log দেখো             |
| `docker logs -f <name>`                                   | Realtime log         |
| `docker exec -it <name> sh`                               | Container ভেতরে ঢোকো |

### 🎼 Docker Compose Commands

| Command                                         | কি করে                     |
| :---------------------------------------------- | :------------------------- |
| `docker compose up -d`                          | চালু করো (background)      |
| `docker compose up --build -d`                  | rebuild করে চালু           |
| `docker compose up --build --force-recreate -d` | পুরো fresh rebuild + চালু  |
| `docker compose down`                           | বন্ধ + container delete    |
| `docker compose stop`                           | শুধু বন্ধ (container থাকে) |
| `docker compose start`                          | বন্ধ container আবার চালু   |
| `docker compose logs -f`                        | Realtime log               |
| `docker compose ps`                             | Compose containers দেখো    |
| `docker compose exec <service> sh`              | Service ভেতরে ঢোকো         |
| `docker compose build --no-cache`               | Cache ছাড়া build          |

### 🧹 Cleanup Commands

| Command                  | কি করে                          |
| :----------------------- | :------------------------------ |
| `docker system df`       | Docker কতটুকু space নিচ্ছে দেখো |
| `docker system prune -a` | সব অব্যবহৃত জিনিস delete        |
| `docker volume prune`    | অব্যবহৃত volume delete          |

---

---

# 🏁 শেষ কথা

### তোমার প্রতিটা Project-এ যা করবে (Checklist):

```
□ .dockerignore তৈরি করো
□ Dockerfile তৈরি করো
□ docker-compose.yml তৈরি করো
□ (React Vite হলে) vite.config.js update করো
□ docker compose up -d দিয়ে চালু করো
□ Browser-এ check করো
□ প্রতিদিন up -d / down — ব্যস!
```

### মনে রাখো:

```
Docker complex না — শুধু নতুন।
তুমি একবার npm শেখার সময়ও confused ছিলে,
এখন npm তো পানির মতো সোজা লাগে, তাই না?
Docker-ও ঠিক তেমন — ২-৩ দিন ব্যবহার করলেই অভ্যাস হয়ে যাবে। 🐳
```
