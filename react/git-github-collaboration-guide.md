# Git & GitHub Collaboration Mastery Guide (Bangla) 🐙

এই গাইডটি আপনাকে একজন **Solo Developer** থেকে **Team Player** এ রূপান্তর করবে। রিমোট জবে কোডিংয়ের চেয়েও বেশি গুরুত্বপূর্ণ হলো টিমের সাথে মিলে কাজ করা, আর তার জন্য Git এবং GitHub এর অ্যাডভান্সড নলেজ অপরিহার্য।

---

## ১. কেন এটি রিমোট জবের জন্য ক্রিটিকাল?

অফিসে আপনি কলিগকে ডেকে বলতে পারেন "আমার কোডটা দেখো", কিন্তু রিমোট জবে সব কমিউনিকেশন হয় **Pull Request (PR)** এবং **Code Review** এর মাধ্যমে। আপনি যদি Git এ কাঁচা হন, তবে আপনি টিমের জন্য বোঝা হয়ে দাঁড়াবেন।

**রিমোট জবে যা যা লাগবে:**

1.  **Branching Strategy:** মেইন ব্রাঞ্চে সরাসরি পুশ না করা।
2.  **Pull Requests (PR):** নিজের কাজ রিভিউয়ের জন্য সাবমিট করা।
3.  **Merge Conflicts:** কনফ্লিক্ট ভয় না পেয়ে সলভ করা।
4.  **Code Review:** অন্যের কোড পড়ে ফিডব্যাক দেওয়া।

---

## ২. প্রফেশনাল সেটআপ (Professional Setup)

### SSH Key সেটআপ (বারবার পাসওয়ার্ড না চাওয়ার জন্য)

HTTPS এর বদলে SSH ব্যবহার করা সিকিউর এবং ফাস্ট।

1.  টার্মিনালে লিখুন: `ssh-keygen -t ed25519 -C "your_email@example.com"`
2.  এন্টার চাপুন (ডিফল্ট লোকেশনে সেভ হবে)।
3.  পাবলিক কি কপি করুন: `cat ~/.ssh/id_ed25519.pub`
4.  GitHub -> Settings -> SSH and GPG keys -> New SSH key তে গিয়ে পেস্ট করুন।

### গ্লোবাল কনফিগারেশন

```bash
git config --global user.name "Omar Faruk"
git config --global user.email "omar@example.com"
git config --global init.defaultBranch main
git config --global core.editor "code --wait" # VS Code কে ডিফল্ট এডিটর বানাতে
```

---

## ৩. গিট ফ্লো (Git Workflow for Teams)

টিমে কাজ করার সময় আমরা **Feature Branch Workflow** ফলো করি।

### ধাপ ১: মেইন ব্রাঞ্চ থেকে লেটেস্ট কোড নেওয়া

কাজ শুরুর আগে সবসময় `main` বা `develop` ব্রাঞ্চ পুল করে নিন।

```bash
git checkout main
git pull origin main
```

### ধাপ ২: নতুন ব্রাঞ্চ তৈরি করা

কাজের ধরণ অনুযায়ী ব্রাঞ্চের নাম দিন।

- Feature: `feat/login-page`
- Bug Fix: `fix/navbar-responsive`
- Chore: `chore/update-dependencies`

```bash
git checkout -b feat/login-page
```

### ধাপ ৩: কাজ করা এবং কমিট করা (Conventional Commits)

কমিট মেসেজ দেখে যেন বোঝা যায় আপনি কি করেছেন। **Conventional Commits** ফলো করা ভালো প্র্যাকটিস।

- `feat: add login form validation`
- `fix: resolve crash on submit`
- `docs: update readme`
- `style: format code with prettier`
- `refactor: optimize image loading`

```bash
git add .
git commit -m "feat: add login form validation with zod"
```

### ধাপ ৪: গিটহাবে পুশ করা

```bash
git push -u origin feat/login-page
```

---

## ৪. পুল রিকোয়েস্ট (Pull Request - PR)

কোড পুশ করার পর GitHub এ গিয়ে **Compare & pull request** এ ক্লিক করুন।

### একটি ভালো PR এর বৈশিষ্ট্য:

1.  **Title:** কাজের সারসংক্ষেপ (যেমন: "Feat: Add Login Page Integration")।
2.  **Description:**
    - **What:** কি কাজ করেছেন?
    - **Why:** কেন করেছেন?
    - **How to Test:** রিভিউয়ার কিভাবে এটি টেস্ট করবে?
    - **Screenshots:** (UI চেঞ্জ হলে অবশ্যই স্ক্রিনশট বা ভিডিও দিন)।
3.  **Link Issue:** যদি জিরা বা গিটহাব ইস্যু থাকে, লিঙ্ক করে দিন (Closes #123)।

> **টিপ:** নিজের PR নিজে একবার রিভিউ করুন (Self-Review) তারপর অন্যদের ট্যাগ করুন।

---

## ৫. মার্জ কনফ্লিক্ট (Merge Conflicts) - ভয়ের কিছু নেই!

যখন আপনি এবং আপনার কলিগ একই ফাইলের একই লাইনে কাজ করেন, তখন কনফ্লিক্ট হয়।

### কিভাবে সলভ করবেন?

১. মেইন ব্রাঞ্চের লেটেস্ট কোড আপনার ব্রাঞ্চে নিয়ে আসুন:

```bash
git checkout main
git pull origin main
git checkout feat/login-page
git merge main # অথবা git rebase main (অ্যাডভান্সড)
```

২. VS Code এ কনফ্লিক্ট ফাইলগুলো লাল হয়ে থাকবে। ফাইলে যান, দেখবেন:

```
<<<<<<< HEAD
Current Change (আপনার কোড)
=======
Incoming Change (মেইন ব্রাঞ্চের কোড)
>>>>>>> main
```

৩. **"Accept Current Change"** বা **"Accept Incoming Change"** বা ম্যানুয়ালি এডিট করে ঠিক করুন।

৪. কনফ্লিক্ট সলভ হলে:

```bash
git add .
git commit -m "fix: resolve merge conflicts"
git push
```

---

## ৬. অ্যাডভান্সড গিট কমান্ড (Advanced Commands)

### ১. `git stash` (কাজ লুকিয়ে রাখা)

হঠাৎ অন্য ব্রাঞ্চে যেতে হবে কিন্তু বর্তমান কাজ কমিট করার মতো অবস্থায় নেই?

```bash
git stash # কাজ সেভ করে ক্লিন করে দেবে
git stash pop # আবার কাজ ফিরিয়ে আনবে
```

### ২. `git reset` (ভুল শুধরানো)

ভুল করে কমিট করে ফেলেছেন?

```bash
git reset --soft HEAD~1 # কমিট ডিলিট হবে কিন্তু কোড থাকবে
git reset --hard HEAD~1 # কমিট এবং কোড সব ডিলিট (সাবধান!)
```

### ৩. `git rebase` (ক্লিন হিস্ট্রি)

মার্জ কমিট এড়াতে রিবেস ব্যবহার করা হয়। এটি আপনার কমিটগুলোকে মেইন ব্রাঞ্চের কমিটের পরে নিয়ে বসিয়ে দেয়।

```bash
git checkout feat/login-page
git rebase main
```

_(নোট: পাবলিক ব্রাঞ্চে রিবেস করবেন না, শুধু নিজের ফিচার ব্রাঞ্চে করবেন)_

---

## ৭. GitHub Actions (Basic CI/CD)

রিমোট জবে প্রায়ই দেখবেন কোড পুশ করলে অটোমেটিক টেস্ট রান হয়। এটি GitHub Actions দিয়ে করা হয়।

`.github/workflows/ci.yml` ফাইলে এটি থাকে।

```yaml
name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm install
      - run: npm test
      - run: npm run lint
```

এর মানে: প্রতি পুশে অটোমেটিক টেস্ট এবং লিন্ট চেক হবে। যদি ফেইল করে, তবে মার্জ করা যাবে না।

---

## ৮. ইন্টারভিউ প্রশ্ন (Interview Questions)

**Q: `git merge` এবং `git rebase` এর পার্থক্য কি?**
A: `git merge` একটি নতুন "merge commit" তৈরি করে এবং হিস্ট্রি যেমন আছে তেমনই রাখে। `git rebase` হিস্ট্রি রি-রাইট করে এবং লিনিয়ার (সোজা) রাখে, কোনো মার্জ কমিট থাকে না।

**Q: আপনি কিভাবে বড় কনফ্লিক্ট হ্যান্ডেল করেন?**
A: আমি প্রথমে কলিগের সাথে কথা বলি (যদি দরকার হয়), তারপর ফাইল বাই ফাইল চেক করে লজিক ঠিক রাখি। তাড়াহুড়ো করে "Accept Incoming" দেই না।

---

## ৯. চেকলিস্ট (Job Ready Checklist)

- [ ] আমি টার্মিনাল দিয়ে গিট ব্যবহার করতে পারি (GUI ছাড়া)।
- [ ] আমি কনফ্লিক্ট সলভ করতে পারি।
- [ ] আমি মিনিংফুল কমিট মেসেজ লিখতে পারি।
- [ ] আমি পুল রিকোয়েস্ট (PR) তৈরি এবং রিভিউ করতে পারি।
- [ ] আমি `.gitignore` ফাইল কনফিগার করতে পারি (node_modules বা .env বাদ দিতে)।

---

## ১০. Git Hooks এবং Automation

ম্যানুয়ালি কোড ফরম্যাট করা বা টেস্ট রান করা মনে থাকে না? Git Hooks দিয়ে এই কাজগুলো অটোমেট করা যায়।

### Git Hooks কি এবং কেন দরকার

Git Hooks হলো এমন স্ক্রিপ্ট যা Git এর কিছু ইভেন্টের (যেমন commit, push) আগে বা পরে রান হয়।

- **Pre-commit hooks:** কমিট করার ঠিক আগে রান হয় (Code formatting, Linting এর জন্য)।
- **Pre-push hooks:** পুশ করার আগে রান হয় (Unit Test রান করার জন্য)।
- **Commit-msg hooks:** কমিট মেসেজ ভ্যালিড কিনা চেক করতে।

### Husky Setup (Modern Standard)

Husky দিয়ে খুব সহজে গিট হুক সেটআপ করা যায়।

১. প্রোজেক্টে Husky ইন্সটল করুন:

```bash
npm install --save-dev husky
```

২. Husky ইনিশিয়ালাইজ করুন:

```bash
npx husky-init && npm install
```

এটি `.husky` ফোল্ডার তৈরি করবে এবং `pre-commit` হুক সেট করবে।

৩. `pre-commit` হুক এডিট করুন:
`.husky/pre-commit` ফাইলে `npm test` এর বদলে আপনার কমান্ড দিন (যেমন ESLint + Prettier):

```bash
#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

npm run lint
npm run format
```

### lint-staged Integration

পুরো প্রজেক্টের সব ফাইলে লিন্ট রান না করে, শুধু যে ফাইলগুলো কমিট করছেন (Staged files) সেগুলোতে রান করতে `lint-staged` ব্যবহার করুন।

১. ইন্সটল:

```bash
npm install --save-dev lint-staged
```

২. `package.json` এ কনফিগারেশন যোগ করুন:

```json
"lint-staged": {
  "**/*.{js,jsx,ts,tsx}": [
    "eslint --fix",
    "prettier --write"
  ]
}
```

৩. `.husky/pre-commit` ফাইল আপডেট করুন:

```bash
npx lint-staged
```

এখন কমিট করলেই অটোমেটিক কোড ফিক্স এবং ফরম্যাট হয়ে যাবে! ✨

---

## ১১. Advanced Git Workflows

### GitHub Flow vs GitFlow vs Trunk-Based

| Workflow        | কখন ব্যবহার করবেন?          | বর্ণনা                                                                             |
| :-------------- | :-------------------------- | :--------------------------------------------------------------------------------- |
| **GitHub Flow** | ছোট টিম, স্টার্টআপ          | মেইন ব্রাঞ্চ থেকে ফিচার ব্রাঞ্চ -> PR -> Merge to Main -> Deploy। সহজ এবং ফাস্ট।   |
| **GitFlow**     | এন্টারপ্রাইজ, রিলিজ সাইকেল  | `main`, `develop`, `feature/*`, `release/*`, `hotfix/*` অনেক ব্রাঞ্চ থাকে।         |
| **Trunk-Based** | হাই ভেলসিটি টিম (Google/FB) | সবাই সরাসরি মেইন ব্রাঞ্চে ছোট ছোট কমিট করে (Feature flags দিয়ে কন্ট্রোল করা হয়)। |

> **Recommendation:** শুরুতে **GitHub Flow** ব্যবহার করুন।

### Branch Protection Rules

টিমে কাজ করলে `main` ব্রাঞ্চ প্রোটেক্টেড রাখা বাধ্যতামূলক।
GitHub Repo -> Settings -> Branches -> Add branch protection rule:

1. **Branch name pattern:** `main`
2. **Require a pull request before merging:** টিক দিন।
3. **Require status checks to pass:** টিক দিন (CI/CD থাকলে)।

এর ফলে কেউ চাইলেও `main` ব্রাঞ্চে সরাসরি পুশ করতে পারবে না, PR লাগবেই।

### Release Management

ভার্সনিং এর জন্য **Semantic Versioning** (v1.2.3) ব্যবহার করুন।

- **Major (1.x.x):** Breaking changes.
- **Minor (x.1.x):** New features (backward compatible).
- **Patch (x.x.1):** Bug fixes.

**Git Tag তৈরি করা:**

```bash
git tag -a v1.0.0 -m "First production release"
git push origin v1.0.0
```

গিটহাবে "Releases" সেকশনে গিয়ে এই ট্যাগ থেকে অটোমেটিক রিলিজ নোট জেনারেট করা যায়।

---

## ১২. Code Review Best Practices

কোড রিভিউ শুধু ভুল ধরা না, নলেজ শেয়ারিং এর মাধ্যম।

### ভালো PR বানানোর নিয়ম

- **Small is Better:** ৪০০ লাইনের বেশি কোড রিভিউ করা কঠিন। ফিচার ছোট ছোট ভাগে ভাগ করুন।
- **Self-Review:** রিভিউয়ারকে ডাকার আগে নিজের PR নিজে একবার দেখুন। `console.log` বা কমেন্টেড কোড আছে কিনা চেক করুন।
- **Template:** PR ডেসক্রিপশন খালি রাখবেন না। কি কাজ করেছেন এবং কিভাবে টেস্ট করতে হবে লিখুন।

### Code Review দেওয়ার নিয়ম

- **Be Constructive:** "This code is bad" না বলে বলুন "Can we improve this logic to be more performant?"
- **Nitpicking:** ভেরিয়েবল নেমিং বা স্পেস নিয়ে বেশি তর্ক করবেন না (এগুলো Linter এর কাজ)। লজিক এবং আর্কিটেকচারে ফোকাস করুন।
- **Emoji Use:**
  - ✅ (Approved)
  - ❓ (Question)
  - 💡 (Suggestion/Idea)
  - 🚀 (Performance improvement)
  - ♻️ (Refactor needed)

### Code Review নেওয়ার নিয়ম

- **ডিফেন্সিভ হবেন না:** রিভিউয়ার আপনার কোডের ভুল ধরছে, আপনার না।
- **Questions:** কনফিউশন থাকলে প্রশ্ন করুন।
- **Update:** চেঞ্জ করে কমেন্টে জানান "Fixed" এবং রি-রিকয়েস্ট দিন।

---

## ১৩. Git History Management

### Interactive Rebase (Squashing Commits)

অনেকগুলো ছোট ছোট কমিট ("fix typo", "fix again", "done") হিস্ট্রি নোংরা করে। মার্জ করার আগে এগুলোকে একটা কমিটে আনা ভালো।

```bash
git rebase -i HEAD~3
```

এডিটর ওপেন হবে। প্রথমটা `pick` রেখে বাকিগুলো `squash` বা `s` করে দিন। সেভ করলে সব মিলে একটা কমিট হবে।

### Git Revert vs Reset

**Reset:** হিস্ট্রি মুছে ফেলে। (Dangerous for public branches)
**Revert:** ভুলের জন্য উল্টো কাজ করে নতুন কমিট তৈরি করে। (Safe)

**কখন কোনটা?**

- নিজের লোকাল ব্রাঞ্চে ভুল হলে: `git reset`
- মেইন ব্রাঞ্চে বা পুশ করা ব্রাঞ্চে ভুল হলে: `git revert`

```bash
git revert <commit-hash>
```

### Cherry-picking

অন্য ব্রাঞ্চ থেকে স্পেসিফিক একটা কমিট আপনার ব্রাঞ্চে আনতে চাইলে:

```bash
git cherry-pick <commit-hash>
```

ধরুন `dev` ব্রাঞ্চে একটা বাগ ফিক্স হয়েছে যা আপনার `feat` ব্রাঞ্চেও দরকার, তখন এটি খুব কাজে লাগে।

---

## ১৪. Team Collaboration Scenarios

### Scenario 1: হঠাৎ production bug fix করতে হবে

আপনি `feat/new-ui` তে কাজ করছেন, হঠাৎ `main` এ ক্রিটিকাল বাগ।
১. বর্তমান কাজ স্ট্যাশ করুন: `git stash`
২. মেইন ব্রাঞ্চে যান: `git checkout main` && `git pull`
৩. হটফিক্স ব্রাঞ্চ বানান: `git checkout -b hotfix/login-error`
৪. ফিক্স করে পুশ ও মার্জ করুন।
৫. আবার নিজের ব্রাঞ্চে ফিরে স্ট্যাশ পপ করুন: `git checkout feat/new-ui` && `git stash pop`

### Scenario 2: Long-running feature branch

আপনার ফিচার শেষ করতে ১০ দিন লাগবে। এর মধ্যে `main` ব্রাঞ্চ অনেক এগিয়ে গেছে।
**Solution:** প্রতিদিন সকালে কাজ শুরুর আগে `git pull origin main` এবং `git rebase main` (বা `merge`) করুন। এতে শেষে গিয়ে বিশাল কনফ্লিক্ট হবে না।

### Scenario 3: Accidentally pushed to main

ভুল করে মেইন ব্রাঞ্চে কমিট করে ফেলেছেন (যদি প্রোটেকশন না থাকে)।
১. লোকালি মেইন ব্রাঞ্চ রিসেট করুন (আগের অবস্থায়): `git reset --hard origin/main`
২. আপনার চেঞ্জগুলো দিয়ে নতুন ব্রাঞ্চ করুন: `git checkout -b feat/my-work`
৩. পুশ করুন।

---

## ১৫. GitHub Advanced Features

### GitHub CLI (gh)

টার্মিনাল থেকেই গিটহাব কন্ট্রোল করা যায়।
**Installation:** `brew install gh` (Mac) বা `sudo apt install gh` (Linux/Windows)

**Login:** `gh auth login`

**PR তৈরি করা:**

```bash
gh pr create --title "Feat: Add Dashboard" --body "Added dashboard layout"
```

**PR চেকআউট করা:**
অন্যের PR লোকালি টেস্ট করতে:

```bash
gh pr checkout 123
```

### GitHub Projects (Kanban Board)

Trello বা Jira এর মতো GitHub এর নিজস্ব প্রোজেক্ট ম্যানেজমেন্ট টুল আছে।

- **Issues:** বাগ বা ফিচারের টিকিট।
- **Board:** Todo, In Progress, Done কলাম।
- **Automation:** PR মার্জ হলে অটোমেটিক "Done" কলামে চলে যায়।

---

এই গাইডটি ফলো করলে আপনি যেকোনো রিমোট টিমে কাজ করার জন্য ১০০% প্রস্তুত। 🚀
