# 🚀 JavaScript Mastery Guide: Zero to Job-Ready (Bangla)

এই গাইডটি আপনাকে জাভাস্ক্রিপ্টের একদম বেসিক থেকে শুরু করে একজন জুনিয়র ফ্রন্টএন্ড ডেভেলপার হিসেবে চাকরির জন্য প্রস্তুত করতে সাহায্য করবে। বিশেষ করে React, Next.js এবং মডার্ন ফ্রেমওয়ার্কের জন্য যে বিষয়গুলো জানা অপরিহার্য, সেগুলোর ওপর এখানে ফোকাস করা হয়েছে।

---

## 1. JavaScript Fundamentals (ভিত্তি মজবুত করা)

জাভাস্ক্রিপ্ট শেখার শুরুতেই এই বিষয়গুলো পরিষ্কার ধারণা থাকা জরুরি।

### Variables: `let`, `const` (কেন `var` নয়?)

আগে `var` ব্যবহার করা হতো, কিন্তু এখন আমরা `let` এবং `const` ব্যবহার করি।

- **`const`**: যখন আপনি জানেন যে ভেরিয়েবলের মান পরিবর্তন হবে না। এটি ডিফল্ট হিসেবে ব্যবহার করা উচিত।
- **`let`**: যখন মান পরিবর্তন করার প্রয়োজন হবে (যেমন লুপ কাউন্টার বা স্টেট আপডেট)।
- **`var` ❌**: এটি ব্যবহার করবেন না। কারণ `var` এর স্কোপিং সমস্যা আছে (function scoped) যা বাগের সৃষ্টি করতে পারে।

```javascript
// ✅ সঠিক পদ্ধতি
const apiBaseUrl = "https://api.example.com"; // পরিবর্তন হবে না
let userCount = 0; // পরিবর্তন হতে পারে
userCount = 1;

// ❌ ভুল পদ্ধতি
var name = "Omar"; // এড়িয়ে চলুন
```

### Data Types: Primitives vs Reference Types

- **Primitives** (মান কপি হয়): String, Number, Boolean, null, undefined, Symbol.
- **Reference Types** (মেমোরি রেফারেন্স শেয়ার করে): Object, Array, Function.

```javascript
// Primitive Example
let a = 10;
let b = a;
b = 20;
console.log(a); // 10 (a পরিবর্তন হয়নি)

// Reference Example
const user1 = { name: "Ali" };
const user2 = user1; // রেফারেন্স কপি হলো
user2.name = "Hassan";
console.log(user1.name); // "Hassan" (user1 ও পরিবর্তন হয়ে গেছে!)
```

### Type Coercion & Comparison (`==` vs `===`)

সবসময় `===` (Strict Equality) ব্যবহার করবেন। এটি ভ্যালু এবং টাইপ দুটোই চেক করে। `==` শুধুমাত্র ভ্যালু চেক করে এবং টাইপ কনভার্ট করে ফেলে, যা অনাকাঙ্ক্ষিত ফলাফল দিতে পারে।

```javascript
const num = 5;
const str = "5";

console.log(num == str); // true (টাইপ ইগনোর করেছে, এটা খারাপ)
console.log(num === str); // false (টাইপ চেক করেছে, এটা নিরাপদ) ✅
```

### Truthy/Falsy Values

কন্ডিশনাল লজিকের জন্য এটি খুব গুরুত্বপূর্ণ।
**Falsy Values**: `false`, `0`, `""` (empty string), `null`, `undefined`, `NaN`.
বাকি সব **Truthy**।

```javascript
const user = null;
if (user) {
  console.log("User found");
} else {
  console.log("No user"); // এটি প্রিন্ট হবে কারণ null হলো falsy
}
```

---

## 2. Functions (কোর কনসেপ্ট)

### Function Declarations vs Expressions vs Arrow Functions

মডার্ন জাভাস্ক্রিপ্টে **Arrow Functions** স্ট্যান্ডার্ড, বিশেষ করে React এ।

```javascript
// 1. Function Declaration (পুরানো স্টাইল, তবে হোইস্টিং কাজ করে)
function add(a, b) {
  return a + b;
}

// 2. Arrow Function (মডার্ন স্টাইল) ✅
const addModern = (a, b) => {
  return a + b;
};

// Implicit Return (এক লাইনে লিখলে return কিওয়ার্ড লাগে না)
const multiply = (a, b) => a * b;
```

### Parameters & Default Values

ফাংশনে যদি আর্গুমেন্ট না পাঠানো হয়, তবে ডিফল্ট ভ্যালু সেট করা যায়।

```javascript
const greet = (name = "Guest") => {
  return `Hello, ${name}!`;
};
console.log(greet()); // "Hello, Guest!"
```

### Higher-Order Functions & Callbacks

যে ফাংশন অন্য ফাংশনকে আর্গুমেন্ট হিসেবে নেয় অথবা ফাংশন রিটার্ন করে, তাকে Higher-Order Function বলে।

```javascript
// Callback Example
const processData = (data, callback) => {
  const processed = data.toUpperCase();
  callback(processed);
};

processData("hello", (result) => {
  console.log("Result:", result); // Result: HELLO
});
```

---

## 3. Arrays (React এ প্রতিনিয়ত লাগবে)

লুপ (`for`, `while`) এর বদলে মডার্ন অ্যারে মেথড ব্যবহার করুন। এগুলো কোডকে ক্লিন রাখে।

### Modern Array Methods

React এ `map`, `filter`, `reduce`, `find` সবচেয়ে বেশি ব্যবহৃত হয়।

#### `map` (লিস্ট রেন্ডার করার জন্য)

অ্যারের প্রতিটি উপাদানের ওপর ফাংশন চালিয়ে নতুন অ্যারে তৈরি করে।

```javascript
const products = [
  { id: 1, name: "Laptop", price: 1000 },
  { id: 2, name: "Phone", price: 500 },
];

// React এ যেভাবে লিস্ট দেখাবেন
const productNames = products.map((product) => product.name);
console.log(productNames); // ["Laptop", "Phone"]
```

#### `filter` (ডাটা ফিল্টার করার জন্য)

শর্ত পূরণ করা উপাদানগুলো নিয়ে নতুন অ্যারে দেয়।

```javascript
const cheapProducts = products.filter((product) => product.price < 800);
// [{ id: 2, name: "Phone", price: 500 }]
```

#### `find` (নির্দিষ্ট আইটেম খোঁজার জন্য)

প্রথম যে উপাদানটি শর্ত পূরণ করে সেটি রিটার্ন করে।

```javascript
const phone = products.find((product) => product.id === 2);
// { id: 2, name: "Phone", price: 500 }
```

### Spread Operator `[...array]`

অ্যারে মিউটেট (সরাসরি পরিবর্তন) না করে নতুন আইটেম যোগ করতে এটি ব্যবহৃত হয়। React State আপডেটে এটি মাস্ট।

```javascript
const numbers = [1, 2, 3];
const newNumbers = [...numbers, 4]; // [1, 2, 3, 4]
// numbers অ্যারে ঠিক আগের মতোই আছে
```

---

## 4. Objects (API এবং State এর জন্য)

### Object Destructuring & Spread

অবজেক্ট থেকে ডাটা বের করা এবং কপি করার মডার্ন উপায়।

```javascript
const user = {
  id: 1,
  name: "Rahim",
  details: {
    age: 25,
    city: "Dhaka",
  },
};

// Destructuring (Extracting values)
const {
  name,
  details: { city },
} = user;
console.log(name, city); // Rahim, Dhaka

// Spread (Updating object immutably) - React এ খুব গুরুত্বপূর্ণ
const updatedUser = {
  ...user,
  name: "Karim", // নাম আপডেট হবে, বাকি সব ঠিক থাকবে
};
```

### Optional Chaining (`?.`)

নেস্টেড অবজেক্ট এক্সেস করার সময় এরর এড়ানোর জন্য। API রেসপন্স হ্যান্ডলিংয়ে এটি জীবন বাঁচায়।

```javascript
const user = { name: "Sokina" };
// console.log(user.address.city); // ❌ Error দিবে কারণ address নেই
console.log(user.address?.city); // ✅ undefined রিটার্ন করবে, অ্যাপ ক্র্যাশ করবে না
```

---

## 5. Modern JavaScript (ES6+ Features)

### Template Literals

স্ট্রিং কনক্যাটিনেশন (`+`) এর বদলে ব্যাকটিক (`` ` ``) ব্যবহার করুন।

```javascript
const name = "World";
// Old
console.log("Hello " + name);
// New ✅
console.log(`Hello ${name}`);
```

### Nullish Coalescing (`??`)

`||` এর চেয়ে নিরাপদ যখন আপনি `0` বা `false` কে ভ্যালিড ভ্যালু হিসেবে রাখতে চান।

```javascript
const count = 0;
const result = count || 10; // 10 (ভুল, কারণ 0 কে falsy ধরেছে)
const resultFixed = count ?? 10; // 0 (সঠিক, শুধুমাত্র null/undefined হলে 10 হবে)
```

---

## 6. Asynchronous JavaScript (API কল করার জন্য)

React এ ডাটা ফেচ করার জন্য এটি বুঝা ফরজ।

### Async/Await (Modern Standard)

`Promise.then()` চেইন এর চেয়ে `async/await` কোড পড়া সহজ করে দেয়। এটি দেখতে synchronous কোডের মতো।

```javascript
// ✅ সঠিক পদ্ধতি: Async/Await এবং Try/Catch
const fetchUserData = async (userId) => {
  try {
    const response = await fetch(`https://api.example.com/users/${userId}`);

    if (!response.ok) {
      throw new Error("Data fetching failed");
    }

    const data = await response.json();
    return data;
  } catch (error) {
    console.error("Error:", error.message);
    return null; // বা এরর হ্যান্ডেল করুন
  }
};
```

### Promise.all()

একাধিক API কল একসাথে করার জন্য।

```javascript
const [user, posts] = await Promise.all([
  fetch("/api/user"),
  fetch("/api/posts"),
]);
```

---

## 7. DOM Manipulation (বেসিক ধারণা)

React এ আমরা সরাসরি DOM ধরি না (Virtual DOM কাজ করে), কিন্তু বেসিক জানা থাকা ভালো।

```javascript
// Element ধরা
const btn = document.querySelector(".submit-btn");

// Event Listener
btn.addEventListener("click", () => {
  console.log("Clicked!");
});

// Class Manipulation
btn.classList.add("active");
```

---

## 8. Professional Code Organization

প্রজেক্ট ফোল্ডার স্ট্রাকচার কেমন হওয়া উচিত:

```
/src
  /components    # React Components (Button, Header)
  /pages         # Next.js Pages
  /utils         # ছোট হেল্পার ফাংশন (formatDate, calculateTax)
  /services      # API কল করার ফাংশন (authService, userService)
  /hooks         # Custom React Hooks
  /constants     # ফিক্সড ভ্যালু (API_URL, THEME_COLORS)
  /types         # TypeScript Types/Interfaces
```

**Best Practice:**

- **Named Exports** ব্যবহার করুন, এতে ইম্পোর্ট করার সময় অটো-কমপ্লিট ভালো কাজ করে।
- ম্যাজিক স্ট্রিং বা নাম্বার সরাসরি কোডে না লিখে `constants` ফাইলে রাখুন।

---

## 9. Modern Syntax & Best Practices (Do's & Don'ts)

| ❌ Don't (করবেন না)                | ✅ Do (করবেন)                        |
| ---------------------------------- | ------------------------------------ |
| `var name = "X"`                   | `const name = "X"`                   |
| `function(x) { return x + 1 }`     | `(x) => x + 1` (Simple callbacks)    |
| `str = "Hi " + name`               | `` str = `Hi ${name}` ``             |
| `if (arr.length > 0)`              | `if (arr.length)` (Truthy check)     |
| `obj.prop && obj.prop.next`        | `obj.prop?.next` (Optional chaining) |
| `for (let i=0; i<arr.length; i++)` | `arr.map()` or `arr.forEach()`       |

---

## 10. JavaScript for React Developers

React ডেভেলপার হিসেবে এই প্যাটার্নগুলো আপনার প্রতিদিন লাগবে।

### Conditional Rendering (&& এবং Ternary)

```javascript
// && Operator (যদি true হয় তবে দেখাও)
{
  isLoggedIn && <UserProfile />;
}

// Ternary Operator (if-else এর শর্টকাট)
{
  isLoading ? <Spinner /> : <DataList />;
}
```

### Immutable State Update

React এ স্টেট সরাসরি পরিবর্তন করা পাপ। সবসময় কপি করে আপডেট করতে হয়।

```javascript
// ❌ ভুল
state.users.push(newUser);

// ✅ সঠিক
setState([...state.users, newUser]);
```

### Object Property Shorthand

```javascript
const name = "Omar";
const age = 25;

// যদি key এবং value এর ভেরিয়েবল নাম same হয়
const user = { name, age }; // { name: "Omar", age: 25 }
```

---

## 11. Common Patterns & Real-World Examples

### Complete Data Fetching Example

```javascript
// services/api.js
export const getProducts = async () => {
  try {
    const res = await fetch("https://dummyjson.com/products");
    if (!res.ok) throw new Error("Failed to fetch");
    const data = await res.json();

    // ডাটা ট্রান্সফর্মেশন (ফ্রন্টএন্ডের জন্য ক্লিন করা)
    return data.products.map((p) => ({
      id: p.id,
      title: p.title,
      price: `$${p.price}`, // ফরম্যাটিং
      inStock: p.stock > 0,
    }));
  } catch (error) {
    console.error(error);
    return []; // ক্র্যাশ না করে খালি অ্যারে রিটার্ন করা
  }
};
```

### Search & Filter

```javascript
const searchProducts = (products, query) => {
  if (!query) return products;

  const lowerQuery = query.toLowerCase();
  return products.filter((p) => p.title.toLowerCase().includes(lowerQuery));
};
```

---

## 12. Code Quality & Standards

1. **Naming Conventions**:

   - Variables/Functions: `camelCase` (`getUserData`, `isActive`)
   - Components/Classes: `PascalCase` (`UserProfile`, `AuthService`)
   - Constants: `UPPER_SNAKE_CASE` (`MAX_RETRY_COUNT`)

2. **Avoid Magic Numbers**:

   ```javascript
   // ❌ Bad
   if (role === 1) { ... }

   // ✅ Good
   const ADMIN_ROLE_ID = 1;
   if (role === ADMIN_ROLE_ID) { ... }
   ```

---

## 13. Edge Cases & Gotchas

- **Empty Array is Truthy**: `if ([])` সত্য রিটার্ন করে। অ্যারে খালি কিনা চেক করতে `array.length > 0` চেক করুন।
- **Floating Point Math**: `0.1 + 0.2 !== 0.3`। টাকার হিসাবের জন্য সাবধান।
- **Reference Copy**: অবজেক্ট কপি করার সময় `spread` (...) শুধুমাত্র ১ লেভেল কপি করে (Shallow Copy)। ডিপ কপির জন্য `structuredClone(obj)` ব্যবহার করুন।

---

## 14. Interview-Ready Knowledge

**Q: Closure কি?**
A: যখন কোনো ফাংশন তার বাইরের স্কোপের ভেরিয়েবল মনে রাখতে পারে, এমনকি বাইরের ফাংশনটি এক্সিকিউশন শেষ করার পরেও। React এর `useState` বা `useEffect` ক্লোজারের ওপর ভিত্তি করে কাজ করে।

**Q: `map` vs `forEach` পার্থক্য কি?**
A: `map` নতুন অ্যারে রিটার্ন করে (React রেন্ডারিংয়ে লাগে), `forEach` কিছু রিটার্ন করে না, শুধু লুপ চালায় (সাইড ইফেক্টের জন্য)।

**Q: Hoisting কি?**
A: ভেরিয়েবল এবং ফাংশন ডিক্লারেশন কোড রান করার আগে মেমোরিতে উপরে উঠে যায়। `var` এবং `function` hoist হয়, কিন্তু `let/const` হয় না (Temporal Dead Zone এ থাকে)।

---

## 15. Debugging & Tools

- **`console.log({ variable })`**: অবজেক্ট হিসেবে লগ করলে নাম এবং ভ্যালু দুটোই দেখা যায়।
  ```javascript
  const name = "Test";
  console.log({ name }); // { name: "Test" } - অনেক বেশি রিডেবল
  ```
- **`debugger`**: কোডের মধ্যে `debugger;` লিখলে ব্রাউজার সেখানে পজ করবে এবং আপনি স্টেপ-বাই-স্টেপ চেক করতে পারবেন।
- **Network Tab**: API কল ফেইল করলে ব্রাউজারের Network ট্যাবে গিয়ে রেসপন্স চেক করুন।

---

## 16. Practice Projects (Portfolio Ideas)

এই প্রজেক্টগুলো করলে আপনার কনফিডেন্স বাড়বে:

1. **Todo App with LocalStorage**:

   - CRUD (Create, Read, Update, Delete) অপারেশন শিখবেন।
   - `JSON.stringify` এবং `JSON.parse` দিয়ে ডাটা ব্রাউজারে সেভ করা শিখবেন।

2. **Weather Dashboard (API Integration)**:

   - `fetch` বা `axios` দিয়ে রিয়েল ডাটা আনা।
   - `async/await` এবং এরর হ্যান্ডলিং প্র্যাকটিস হবে।
   - কন্ডিশনাল রেন্ডারিং (লোডিং স্টেট, এরর মেসেজ)।

3. **E-commerce Cart System**:
   - অ্যারে মেথড (`reduce` দিয়ে টোটাল প্রাইস বের করা)।
   - অবজেক্ট ম্যানিপুলেশন (কার্টে আইটেম যোগ/বিয়োগ করা)।

---

## 17. Next Steps

জাভাস্ক্রিপ্ট বেসিক স্ট্রং হলে এরপর যা শিখবেন:

1. **React.js**: কম্পোনেন্ট বেইজড আর্কিটেকচার।
2. **TypeScript**: জাভাস্ক্রিপ্টের সুপারসেট যা টাইপ সেফটি দেয় (চাকরিতে এখন ম্যান্ডেটরি হয়ে যাচ্ছে)।
3. **Next.js**: React এর ফ্রেমওয়ার্ক যা প্রোডাকশন লেভেল অ্যাপের জন্য সেরা।

**Resources:**

- MDN Web Docs (বাইবেল)
- JavaScript.info (গভীর নলেজের জন্য)

---

## ✅ React Readiness Checklist (আপনি কি প্রস্তুত?)

React শুরু করার আগে নিচের বিষয়গুলোতে টিক (✔) দিতে পারলে আপনি প্রস্তুত:

- [ ] আমি `let` এবং `const` এর পার্থক্য বুঝি এবং `var` কেন ব্যবহার করব না তা জানি।
- [ ] আমি `Arrow Function` লিখতে পারি এবং সাধারণ ফাংশনের সাথে এর পার্থক্য বুঝি।
- [ ] আমি `map`, `filter`, `find` দিয়ে অ্যারে ম্যানিপুলেট করতে পারি (লুপ ছাড়া)।
- [ ] আমি `Destructuring` এবং `Spread Operator` (...) ব্যবহার করে অবজেক্ট/অ্যারে কপি করতে পারি।
- [ ] আমি `Async/Await` দিয়ে API থেকে ডাটা আনতে পারি এবং `try/catch` দিয়ে এরর হ্যান্ডেল করতে পারি।
- [ ] আমি `ES6 Modules` (import/export) বুঝি।
- [ ] আমি `Ternary Operator` এবং `&&` দিয়ে কন্ডিশনাল লজিক লিখতে পারি।

যদি সবগুলোতে টিক দিতে পারেন, **অভিনন্দন! আপনি React শেখার জন্য ১০০% প্রস্তুত।** 🚀

---

> **উপদেশ**: কোড মুখস্থ করবেন না। লজিক বুঝুন এবং প্রচুর প্র্যাকটিস করুন। কনসোল ওপেন করে কোড লিখুন। শুভকামনা! 🚀
