# VS Code দিয়ে AWS Server-এ SSH কানেকশন গাইড

## নিরাপদে এবং প্রফেশনালভাবে রিমোট সার্ভার ম্যানেজমেন্ট

---

## 📑 সূচিপত্র

- [VS Code দিয়ে AWS Server-এ SSH কানেকশন গাইড](#vs-code-দিয়ে-aws-server-এ-ssh-কানেকশন-গাইড)
  - [নিরাপদে এবং প্রফেশনালভাবে রিমোট সার্ভার ম্যানেজমেন্ট](#নিরাপদে-এবং-প্রফেশনালভাবে-রিমোট-সার্ভার-ম্যানেজমেন্ট)
  - [📑 সূচিপত্র](#-সূচিপত্র)
  - [SSH কি?](#ssh-কি)
  - [প্রয়োজনীয় রিকুইয়ারমেন্টস](#প্রয়োজনীয়-রিকুইয়ারমেন্টস)
  - [ধাপ ১: VS Code Extension সেটআপ](#ধাপ-১-vs-code-extension-সেটআপ)
  - [ধাপ ২: নতুন হোস্ট (Server) অ্যাড করা](#ধাপ-২-নতুন-হোস্ট-server-অ্যাড-করা)
  - [ধাপ ৩: সার্ভারে কানেক্ট করা](#ধাপ-৩-সার্ভারে-কানেক্ট-করা)
  - [সফল কানেকশন চেনার উপায়](#সফল-কানেকশন-চেনার-উপায়)
  - [সার্ভারে ঢোকার পর প্রথম প্রয়োজনীয় কমান্ড](#সার্ভারে-ঢোকার-পর-প্রথম-প্রয়োজনীয়-কমান্ড)
  - [সতর্কতা: যা করা বিপজ্জনক](#সতর্কতা-যা-করা-বিপজ্জনক)
  - [সার্ভারের ফাইল/ফোল্ডার ওপেন করা](#সার্ভারের-ফাইলফোল্ডার-ওপেন-করা)
  - [কমন সমস্যা এবং সমাধান (FAQ)](#কমন-সমস্যা-এবং-সমাধান-faq)
  - [বেস্ট প্র্যাকটিস](#বেস্ট-প্র্যাকটিস)
  - [পরবর্তী ধাপ (Next Steps)](#পরবর্তী-ধাপ-next-steps)

---

## SSH কি?

- **SSH** এর পূর্ণরূপ হলো **Secure Shell**।
- এটি একটি নেটওয়ার্ক প্রোটোকল যার মাধ্যমে আপনি ইন্টারনেটের সাহায্যে দূরে থাকা কোনো কম্পিউটার বা সার্ভারে (যেমন: AWS EC2) নিরাপদভাবে লগইন করতে পারেন।
- AWS সার্ভার ম্যানেজ করার প্রধান এবং সবচেয়ে নিরাপদ উপায় হলো এই SSH।

---

## প্রয়োজনীয় রিকুইয়ারমেন্টস

সার্ভারে কানেক্ট করার জন্য নিচের জিনিসগুলো আগে থেকে রেডি রাখুন:

- **OS:** Windows 10/11 (আমাদের এই গাইড Windows-এর জন্য)।
- **Tool:** VS Code এবং ইন্টারনেট কানেকশন।
- **সার্ভার ডিটেইলস (Credentials):**
  - **IP Address:** (যেমন: `109.123.232.6`)
  - **User:** (যেমন: `root` অথবা `ubuntu`)
  - **Password** বা **SSH Key (.pem/ .ppk file)**।

---

## ধাপ ১: VS Code Extension সেটআপ

1. VS Code ওপেন করুন।
2. বাম পাশের **Extensions** প্যানেলে যান (Shortcut: `Ctrl + Shift + X`)।
3. সার্চ বক্সে লিখুন: **"Remote - SSH"**।
4. **Microsoft**-এর ডেসক্রিপশনে থাকা এই এক্সটেনশনটি **Install** করুন।

---

## ধাপ ২: নতুন হোস্ট (Server) অ্যাড করা

প্রথমবার কানেক্ট করার সময় আপনাকে হোস্ট অ্যাড করতে হবে:

1. কিবোর্ড থেকে চাপুন: `Ctrl + Shift + P` (Command Palette)।
2. লিখুন: `Remote-SSH: Connect to Host` এবং এন্টার দিন।
3. ড্রপডাউন থেকে **Add New SSH Host** সিলেক্ট করুন।
4. কানেকশন কমান্ড দিন (আপনার ইউজার এবং আইপি অনুযায়ী):
   ```bash
   ssh root@109.123.232.6
   ```
5. আপনার কম্পিউটারের কনফিগারেশন ফাইলটি সিলেক্ট করুন। সাধারণত এটি হয়:
   `C:\Users\<YourUser>\.ssh\config`

---

## ধাপ ৩: সার্ভারে কানেক্ট করা

হোস্ট অ্যাড হয়ে গেলে কানেক্ট করুন:

1. আবার `Ctrl + Shift + P` চাপুন।
2. `Remote-SSH: Connect to Host` লিখে এন্টার দিন।
3. লিস্ট থেকে আপনার আইপি অ্যাড্রেস বা হোস্টটি (যেমন: `109.123.232.6`) সিলেক্ট করুন।
4. একটি নতুন VS Code উইন্ডো ওপেন হবে।
5. **Security Alert:** যদি মেসেজ আসে _"Are you sure you want to continue connecting?"_, তবে **Continue** বা **Yes** দিন।
6. **Password:** সার্ভারের পাসওয়ার্ড চাইলে তা দিন। (মনে রাখবেন: টার্মিনাল বা উইন্ডোতে পাসওয়ার্ড টাইপ করার সময় কোনো স্টার বা ক্যারেক্টার দেখা যাবে না, যা লিনাক্সের সাধারণ নিয়ম)।
7. **OS Platform:** যদি জিজ্ঞেস করে সার্ভার কি ধরণের, তবে **Linux** সিলেক্ট করুন।

---

## সফল কানেকশন চেনার উপায়

সার্ভারে ঠিকভাবে ঢুকেছেন কিনা বুঝতে নিচের দুটি বিষয় চেক করুন:

- **নিচে বাম কোণে:** VS Code-এর স্ট্যাটাস বারে দেখাবে: `SSH: 109.123.232.6`।
- **টার্মিনালে:** টার্মিনাল ওপেন করলে ইউজার আইডি দেখাবে, যেমন: `root@hostname:~#`।

এখন আপনি যা টাইপ করবেন, তা সরাসরি আপনার AWS সার্ভারে রান হবে।

---

## সার্ভারে ঢোকার পর প্রথম প্রয়োজনীয় কমান্ড

কানেক্ট হওয়ার পর সার্ভারের অবস্থা বুঝতে নিচের কমান্ডগুলো চালানো নিরাপদ:

- `pwd`: বর্তমানে আপনি কোন ফোল্ডারে আছেন তা জানতে।
- `ls`: আপনার বর্তমান ফোল্ডারে কি কি ফাইল বা ফোল্ডার আছে তা দেখতে।
- `whoami`: আপনি কোন ইউজার হিসেবে লগইন করেছেন তা নিশ্চিত হতে।
- `cat /etc/os-release`: লিনাক্সের কোন ভার্সন (Ubuntu/Debian/CentOS) ব্যবহার করছেন তা দেখতে।
- `df -h`: সার্ভারের ডিস্ক স্পেস বা স্টোরেজ কতটুকু ফাঁকা আছে তা চেক করতে।

---

## সতর্কতা: যা করা বিপজ্জনক

`root` ইউজার হিসেবে আপনার হাতে সার্ভারের সব ক্ষমতা থাকে। তাই খুব সাবধান! নিচের ভুলগুলো করবেন না:

- ❌ `rm -rf /`: এই কমান্ড ভুল করেও দেবেন না, এটি সার্ভারের সবকিছু ডিলিট করে দেয়।
- ❌ `reboot` বা `shutdown`: পারমিশন ছাড়া সার্ভার রিবুট বা বন্ধ করবেন না।
- ❌ **অচেনা কমান্ড:** ইন্টারনেট থেকে না বুঝে কোনো র‍্যান্ডম কমান্ড কপি-পেস্ট করবেন না।
- ❌ **System Folder Delete:** `/etc`, `/bin`, `/var` ইত্যাদি সিস্টেম ফোল্ডার এডিট বা ডিলিট করবেন না।

---

## সার্ভারের ফাইল/ফোল্ডার ওপেন করা

VS Code দিয়ে সার্ভারের ফাইল সরাসরি এডিট করতে:

1. **File → Open Folder** এ যান।
2. বক্সে পাথ লিখুন (যেমন: `/root` অথবা প্রজেক্ট থাকলে `/var/www/html`)।
3. **OK** দিন। এখন সার্ভারের সব ফাইল আপনার ল্যাপটপের ফাইলের মতোই বাম পাশে এডিটরে দেখতে পাবেন।

---

## কমন সমস্যা এবং সমাধান (FAQ)

| সমস্যা                 | সমাধান                                                                                                                 |
| :--------------------- | :--------------------------------------------------------------------------------------------------------------------- |
| **Wrong Password**     | Caps Lock চেক করুন। অনেক সময় পাসওয়ার্ড কপি-পেস্ট করলে এক্সট্রা স্পেস চলে আসে, ম্যানুয়ালি টাইপ করার চেষ্টা করুন।        |
| **Connection Timeout** | আপনার ইন্টারনেট চেক করুন। ভিপিএন (VPN) চালু থাকলে বন্ধ করে ট্রাই করুন।                                                 |
| **Permission Denied**  | ইউজার নেম ঠিক আছে কিনা চেক করুন (যেমন `root` এর জায়গায় `ubuntu` হতে পারে)।                                             |
| **SSH Key Error**      | যদি পাসওয়ার্ডের বদলে Key (.pem) ফাইল লাগে, তবে SSH command-এ `-i` ফ্ল্যাগ ব্যবহার করতে হয়: `ssh -i "key.pem" root@ip`। |

---

## বেস্ট প্র্যাকটিস

- **Backup:** যে কোনো কনফিগার ফাইল এডিট করার আগে একটি কপি রাখুন।
- **SSH Config:** বারবার কমান্ড না লিখে `C:\Users\<User>\.ssh\config` ফাইলে হোস্ট ডিটেইলস সেভ করে রাখা যায়।
- **Key-based Auth:** পাসওয়ার্ডের চেয়ে SSH Key ব্যবহার করা অনেক বেশি নিরাপদ।

---

## পরবর্তী ধাপ (Next Steps)

সার্ভারে কানেক্ট হওয়ার পর আপনার যা শেখা প্রয়োজন:

- লিনাক্সের বেসিক কমান্ড (Navigation, File ownership)।
- এনজিনএক্স (Nginx) বা কোনো ওয়েব সার্ভার কনফিগারেশন।
- ডকার (Docker) বা গিট (Git) এর মাধ্যমে কোড ডেপ্লয় করা।

---

> [!TIP]
> আপনি যদি এখন লিনাক্স কমান্ড নিয়ে কাজ শুরু করতে চান, আমি আপনার জন্য একটি **"Essential Linux Cheat Sheet"** তৈরি করে দিতে পারি। 💻

---

**প্রস্তুতকারক:** Antigravity (Advanced Coding Agent)  
**তারিখ:** ২ ফেব্রুয়ারি, ২০২৬
