# 🚀 React (Vite) প্রজেক্ট AWS EC2-তে ডেপ্লয় করার পূর্ণাঙ্গ গাইড

এই গাইডটি একদম নতুনদের জন্য তৈরি করা হয়েছে যারা আগে কখনও সার্ভারে কাজ করেননি। আমরা ধাপে ধাপে শিখব কীভাবে আপনার লোকাল কম্পিউটার থেকে একটি React (Vite) প্রজেক্ট GitHub-এর মাধ্যমে সরাসরি AWS EC2 সার্ভারে লাইভ করতে হয়।

---

## 📑 সূচিপত্র

1. [Phase 1: লোকাল প্রজেক্ট সেটআপ (Vite & GitHub)](#phase-1-লোকাল-প্রজেক্ট-সেটআপ-vite--github)
2. [Phase 2: AWS EC2 সার্ভার রেডি করা](#phase-2-aws-ec2-সার্ভার-রেডি-করা)
3. [Phase 3: VS Code SSH দিয়ে সার্ভারে কানেক্ট করা](#phase-3-vs-code-ssh-দিয়ে-সার্ভারে-কানেক্ট-করা)
4. [Phase 4: সার্ভারে প্রয়োজনীয় সফটওয়্যার ইনস্টল করা](#phase-4-সার্ভারে-প্রয়োজনীয়-সফটওয়্যার-ইনস্টল-করা)
5. [Phase 5: প্রজেক্ট ডেপ্লয় এবং Nginx কনফিগারেশন](#phase-5-প্রজেক্ট-ডেপ্লয়-এবং-nginx-কনফিগারেশন)
6. [Phase 6: ডোমেইন এবং SSL সেটআপ (HTTPS)](#phase-6-ডোমেইন-এবং-ssl-সেটআপ-https)
7. [Phase 7: ভবিষ্যতে কোড আপডেট করার নিয়ম](#phase-7-ভবিষ্যতে-কোড-আপডেট-করার-নিয়ম)

---

## Phase 1: লোকাল প্রজেক্ট সেটআপ (Vite & GitHub)

সার্ভারে পাঠানোর আগে আপনার প্রজেক্টটি লোকাল কম্পিউটারে ঠিকভাবে তৈরি এবং GitHub-এ থাকতে হবে।

### ১.১ Vite প্রজেক্ট তৈরি করা

আপনার কম্পিউটারের টার্মিনালে নিচের কমান্ডগুলো দিয়ে একটি নতুন প্রজেক্ট তৈরি করুন:

```bash
# নতুন প্রজেক্ট তৈরি
npm create vite@latest my-react-app -- --template react

# প্রজেক্ট ফোল্ডারে ঢোকা
cd my-react-app

# ডিপেন্ডেন্সি ইনস্টল করা
npm install

# প্রজেক্টটি একবার চালিয়ে চেক করা
npm run dev
```

### ১.২ GitHub-এ কোড পুশ করা

1. GitHub-এ একটি নতুন **Repository** তৈরি করুন (যেমন: `my-react-app`)।
2. আপনার লোকাল টার্মিনালে নিচের কমান্ডগুলো দিন:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/your-repo-name.git
git push -u origin main
```

> [!CAUTION]
> **সম্ভাব্য সমস্যা:** `git push` করার সময় ইউজারনেম বা পাসওয়ার্ড চাইতে পারে। মনে রাখবেন, GitHub এখন পাসওয়ার্ডের বদলে **Personal Access Token (PAT)** ব্যবহার করে। এটি GitHub Settings থেকে জেনারেট করে নিতে হবে।

---

## Phase 2: AWS EC2 সার্ভার রেডি করা

এখন আমরা AWS-এ একটি কম্পিউটার (Instance) ভাড়া নেব যেখানে আমাদের ওয়েবসাইটটি চলবে।

### ২.১ Instance লঞ্চ করা

1. [AWS Console](https://console.aws.amazon.com/)-এ লগইন করুন এবং **EC2** সার্চ করে সেখানে যান।
2. **Launch Instance** বাটনে ক্লিক করুন।
3. **Name:** একটি নাম দিন (যেমন: `react-server`)।
4. **OS (AMI):** **Ubuntu 22.04 LTS** সিলেক্ট করুন (এটি খুবই জনপ্রিয় এবং ফ্রি)।
5. **Instance Type:** `t2.micro` সিলেক্ট করুন (এটি Free Tier eligible)।
6. **Key Pair:** **Create new key pair**-এ ক্লিক করে একটি নাম দিন এবং `.pem` ফাইলটি ডাউনলোড করে নিরাপদ জায়গায় রাখুন। (এটি ছাড়া আপনি সার্ভারে ঢুকতে পারবেন না)।

### ২.২ Security Groups (ফায়ারওয়াল)

এটি আপনার সার্ভারের গেট কিপার। **Network Settings**-এ গিয়ে নিচের পোর্টগুলো খুলে দিন:

- **SSH (Port 22):** আপনার নিজের IP থেকে (সার্ভারে কাজ করার জন্য)।
- **HTTP (Port 80):** সবার জন্য (0.0.0.0/0) - ওয়েবসাইট দেখার জন্য।
- **HTTPS (Port 443):** সবার জন্য (0.0.0.0/0) - সিকিউর কানেকশনের জন্য।

> [!TIP]
> **টিপস:** সিকিউরিটি গ্রুপে নিয়মগুলো যোগ করার পর নিচে **"Save rules"** বাটনে ক্লিক করতে ভুলবেন না, নাহলে ওয়েবসাইট কাজ করবে না।

---

## Phase 3: VS Code SSH দিয়ে সার্ভারে কানেক্ট করা

আমরা PuTTY ব্যবহার না করে সরাসরি VS Code দিয়ে সার্ভারে কাজ করব, যা অনেক বেশি সহজ।

1. VS Code-এ **Remote - SSH** এক্সটেনশনটি ইনস্টল করুন।
2. `Ctrl + Shift + P` চেপে লিখুন `Remote-SSH: Add New SSH Host`।
3. কমান্ড দিন: `ssh -i "path/to/your-key.pem" ubuntu@YOUR_SERVER_IP`
4. কানেক্ট হয়ে গেলে একটি নতুন উইন্ডো খুলবে। নিচে বাম কোণায় **SSH: IP_ADDRESS** লেখা দেখলে বুঝবেন আপনি সফলভাবে কানেক্ট হয়েছেন।

> [!IMPORTANT]
> আপনার `.pem` ফাইলের পাথটি সঠিক হতে হবে। যদি Windows-এ পারমিশন এরর দেয়, তবে কি ফাইলটি `C:\Users\YourName\.ssh` ফোল্ডারে রাখুন।

**সম্ভাব্য সমস্যা:** কানেক্ট করার সময় "Connection Timed Out" দেখালে বুঝতে হবে আপনার ইন্টারনেট বা পোর্টে সমস্যা আছে। অথবা আপনার কম্পিউটারের ফায়ারওয়াল SSH আটকে দিচ্ছে।

---

## Phase 4: সার্ভারে প্রয়োজনীয় সফটওয়্যার ইনস্টল করা

সার্ভারে ঢোকার পর টার্মিনালে একে একে এই কমান্ডগুলো দিয়ে প্রয়োজনীয় পরিবেশ তৈরি করুন।

### ৪.১ সিস্টেম আপডেট

```bash
sudo apt update && sudo apt upgrade -y
```

_উদ্দেশ্য: সার্ভারের অপারেটিং সিস্টেমকে লেটেস্ট ভার্সনে নিয়ে আসা।_

### ৪.২ Node.js ইনস্টল (Vite এর জন্য)

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
```

_উদ্দেশ্য: JavaScript কোড রান করার জন্য Node.js দরকার।_

### ৪.৩ Nginx ইনস্টল (Web Server)

```bash
sudo apt install -y nginx
```

_উদ্দেশ্য: Nginx আপনার React অ্যাপের ফাইলগুলোকে ইন্টারনেটে দেখানোর কাজ করবে।_

---

## Phase 5: প্রজেক্ট ডেপ্লয় এবং Nginx কনফিগারেশন

### ৫.১ কোড ক্লোন করা

```bash
cd /var/www
sudo git clone https://github.com/YOUR_USERNAME/your-repo-name.git
sudo chown -R ubuntu:ubuntu your-repo-name
cd your-repo-name
```

### ৫.২ প্রজেক্ট বিল্ড করা

Vite প্রজেক্ট সরাসরি সার্ভারে রান হয় না, এটি "Build" করতে হয় যা থেকে ছোট ছোট HTML/JS ফাইল তৈরি হয়।

```bash
npm install
npm run build
```

এটি করার পর আপনার প্রজেক্ট ফোল্ডারে একটি **`dist`** ফোল্ডার তৈরি হবে।

> [!WARNING]
> **সম্ভাব্য সমস্যা:** `npm run build` করার সময় যদি "Killed" বা "Out of memory" লেখা আসে, তবে বুঝতে হবে আপনার সার্ভারের র‍্যাম (RAM) শেষ হয়ে গেছে। এক্ষেত্রে নিচের Phase 8 এ দেওয়া **Swap File** মেথড ব্যবহার করুন।

### ৫.৩ Nginx কনফিগার করা

Nginx-কে বলে দিতে হবে যে আমাদের ফাইলগুলো কোথায় আছে।

```bash
sudo nano /etc/nginx/sites-available/react-app
```

এখন নিচের কোডটি কপি করে সেখানে পেস্ট করুন (IP এর জায়গায় আপনার সার্ভার IP দিন):

```nginx
server {
    listen 80;
    server_name YOUR_SERVER_IP;

    root /var/www/your-repo-name/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

_সেভ করতে `Ctrl + O`, তারপর `Enter` এবং বের হতে `Ctrl + X` চাপুন।_

### ৫.৪ সাইটটি চালু করা

```bash
# কনফিগারেশনটি একটিভ করা
sudo ln -s /etc/nginx/sites-available/react-app /etc/nginx/sites-enabled/

# ডিফল্ট কনফিগারেশন বন্ধ করা
sudo rm /etc/nginx/sites-enabled/default

# Nginx রিস্টার্ট করা
sudo systemctl restart nginx
```

এখন ব্রাউজারে আপনার সার্ভার IP দিলে ওয়েবসাইটটি দেখতে পাবেন! 🎉

**সম্ভাব্য সমস্যা:** যদি "403 Forbidden" বা "404 Not Found" আসে, তবে আপনার Nginx কনফিগ ফাইলে `root` পাথটি ঠিক আছে কিনা আবার চেক করুন।

---

## Phase 6: ডোমেইন এবং SSL সেটআপ (HTTPS)

আপনার যদি একটি ডোমেইন থাকে (যেমন: `example.com`), তবে সেটিকে IP-এর বদলে ব্যবহার করা ভালো।

1. আপনার ডোমেইন প্রোভাইডারের প্যানেলে গিয়ে একটি **A Record** অ্যাড করুন যা আপনার সার্ভার IP-কে পয়েন্ট করবে।
2. Nginx কনফিগ ফাইলে `server_name` এর জায়গায় ডোমেইন নামটি দিন।
3. **SSL (ফ্রি) ইনস্টল করতে:**

```bash
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d example.com
```

> [!CAUTION]
> **সম্ভাব্য সমস্যা:** Certbot শুরু করার আগে ডোমেইনটি অবশ্যই আপনার সার্ভার IP-তে পয়েন্ট করা থাকতে হবে। ডোমেইন সেট করার পর অন্তত ১০-১৫ মিনিট অপেক্ষা করুন যাতে DNS আপডেট হয়।

---

## Phase 7: ভবিষ্যতে কোড আপডেট করার নিয়ম

যখন আপনি আপনার কম্পিউটারে নতুন কোনো পরিবর্তন করবেন, তখন এই ৩টি ধাপ অনুসরণ করুন:

**১. আপনার কম্পিউটারে:**

```bash
git add .
git commit -m "Updated some features"
git push origin main
```

**২. সার্ভারে (VS Code Terminal):**

```bash
cd /var/www/your-repo-name
git pull origin main
npm run build
```

**৩. Nginx রিস্টার্ট (প্রয়োজন হলে):**

```bash
sudo systemctl restart nginx
```

---

## 🚫 যা করবেন না (Common Mistakes)

- ❌ **node_modules ফোল্ডার আপলোড:** কখনও GitHub বা সার্ভারে সরাসরি `node_modules` আপলোড করবেন না। সবসময় `npm install` ব্যবহার করবেন।
- ❌ **Security Group বন্ধ রাখা:** যদি ওয়েবসাইট না খোলে, তবে চেক করুন আপনার AWS প্যানেলে Port 80 সবার জন্য খোলা আছে কিনা।
- ❌ **বড় বড় ফাইল রাখা:** EC2 এর t2.micro তে জায়গা কম থাকে, তাই অপ্রয়োজনীয় বড় ফাইল সার্ভারে রাখবেন না।

> [!TIP]
> যদি কখনো সার্ভারের র‍্যাম কমে যায় (Out of memory), তবে একটি **Swap File** তৈরি করে নেওয়া বুদ্ধিমানের কাজ।

---

## Phase 8: সার্ভারের র‍্যাম বাড়ানো (Swap File)

t2.micro তে মাত্র ১ GB র‍্যাম থাকে, যা বড় প্রজেক্ট বিল্ড করতে গিয়ে শেষ হয়ে যেতে পারে। ১ মিনিটের এই কাজটির মাধ্যমে আপনি ভার্চুয়াল র‍্যাম বাড়িয়ে নিতে পারেন:

```bash
# ২ জিবি সোয়াপ ফাইল তৈরি
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile

# পার্মানেন্ট করা
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
```

_উদ্দেশ্য: হার্ডডিস্ক থেকে কিছু জায়গা র‍্যাম হিসেবে ব্যবহার করা।_

---

**গাইডটি তৈরি করেছেন:** Antigravity AI  
**তারিখ:** ১৭ ফেব্রুয়ারি, ২০২৬
